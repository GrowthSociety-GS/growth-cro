// /gsg — loading skeleton (SP-9).
import { PageSkeleton } from "@/components/common/LoadingSkeleton";

export default function GsgLoading() {
  return <PageSkeleton title="GSG studio" cards={2} />;
}
