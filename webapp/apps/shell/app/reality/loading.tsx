// /reality — loading skeleton (SP-9).
import { PageSkeleton } from "@/components/common/LoadingSkeleton";

export default function RealityLoading() {
  return <PageSkeleton title="reality" cards={2} />;
}
