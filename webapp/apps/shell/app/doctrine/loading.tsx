// /doctrine — loading skeleton (SP-9).
import { PageSkeleton } from "@/components/common/LoadingSkeleton";

export default function DoctrineLoading() {
  return <PageSkeleton title="doctrine" cards={3} />;
}
