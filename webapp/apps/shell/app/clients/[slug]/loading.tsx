// /clients/[slug] — loading skeleton (SP-9).
import { DetailSkeleton } from "@/components/common/LoadingSkeleton";

export default function ClientDetailLoading() {
  return <DetailSkeleton title="client detail" />;
}
