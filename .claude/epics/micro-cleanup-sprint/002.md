---
name: Archive growthcro/gsg_lp/ legacy island
status: open
created: 2026-05-12T13:30:00Z
updated: 2026-05-12T13:30:00Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Archive growthcro/gsg_lp/ legacy island

## Description

Archiver `growthcro/gsg_lp/` (7 fichiers : `lp_orchestrator.py`, `mega_prompt_builder.py`, `repair_loop.py`, `brand_blocks.py`, `data_loaders.py`, `README.md`, `__init__.py`) vers `_archive/growthcro_gsg_lp_2026-05-12_legacy_island/` car identifié comme module island sans consumer actif post-cleanup. Précondition critique : vérifier 0 import actif avant move (STOP escalate si import trouvé).

## Acceptance Criteria

- [ ] **PRÉ-CHECK** : 0 import actif de `growthcro.gsg_lp.*` dans les paths actifs :
  ```bash
  grep -rn "from growthcro.gsg_lp\|import growthcro.gsg_lp" \
    growthcro/ moteur_gsg/ moteur_multi_judge/ skills/ scripts/ 2>&1 \
    | grep -v "_archive\|growthcro/gsg_lp/" | head -20
  ```
  Doit retourner 0 ligne. Si match → **STOP**, escalate to user via progress.md note.
- [ ] `git mv growthcro/gsg_lp _archive/growthcro_gsg_lp_2026-05-12_legacy_island/` exécuté
- [ ] `growthcro/gsg_lp/` n'existe plus dans active tree
- [ ] `_archive/growthcro_gsg_lp_2026-05-12_legacy_island/` contient tous les 7 fichiers
- [ ] `python3 scripts/update_architecture_map.py` exécuté (idempotent, regen WEBAPP_ARCHITECTURE_MAP.yaml + MD)
- [ ] `growthcro/gsg_lp/` absent de l'architecture map post-regen :
  ```bash
  grep -c "growthcro/gsg_lp" .claude/docs/state/WEBAPP_ARCHITECTURE_MAP.yaml
  # Expected: 0
  ```
- [ ] `python3 scripts/audit_capabilities.py` orphans HIGH=0
- [ ] `python3 scripts/lint_code_hygiene.py` exit 0 (pas de nouveau FAIL/WARN)
- [ ] `bash scripts/parity_check.sh weglot` exit 0 (108 files baseline)
- [ ] `python3 SCHEMA/validate_all.py` exit 0
- [ ] `bash scripts/typecheck.sh` exit 0
- [ ] 6/6 GSG checks PASS (gsg_lp était presumed-legacy, donc no impact)
- [ ] Commit isolé : `chore(cleanup): archive growthcro/gsg_lp/ legacy island (no active consumer) (#<N>)`

## Technical Details

### Pré-check exhaustif

Le grep doit être exhaustif. Cas à vérifier :
- `from growthcro.gsg_lp import ...`
- `from growthcro.gsg_lp.X import ...`
- `import growthcro.gsg_lp`
- `import growthcro.gsg_lp as ...`
- Toute string dynamique : `getattr(growthcro, 'gsg_lp')` ou `importlib.import_module('growthcro.gsg_lp')`

```bash
# Statique
grep -rn "growthcro.gsg_lp\|growthcro\.gsg_lp" \
  growthcro/ moteur_gsg/ moteur_multi_judge/ skills/ scripts/ 2>&1 \
  | grep -v "^_archive\|^growthcro/gsg_lp" \
  | grep -v "Binary file" \
  | head -30

# Dynamique (recherche string)
grep -rn "'gsg_lp'\|\"gsg_lp\"" \
  growthcro/ moteur_gsg/ moteur_multi_judge/ skills/ scripts/ 2>&1 \
  | grep -v "^_archive\|^growthcro/gsg_lp" \
  | head -20
```

Si **n'importe quel match** trouvé en dehors de `growthcro/gsg_lp/` lui-même → écrire dans `progress.md` :
```markdown
## Blocker
Pré-check failed: imports trouvés à <file:line>
<copy-paste les matches>
Décision Mathis requise : refactor les callsites OU annuler l'archive.
```
Et STOP la task (pas de commit, pas de move).

### Move command

```bash
cd /Users/mathisfronty/Developer/epic-micro-cleanup-sprint
# Verify destination doesn't exist
ls _archive/growthcro_gsg_lp_2026-05-12_legacy_island/ 2>&1 | head -3
# (should be "No such file or directory")

git mv growthcro/gsg_lp _archive/growthcro_gsg_lp_2026-05-12_legacy_island/

# Verify move
ls _archive/growthcro_gsg_lp_2026-05-12_legacy_island/
# Should show: README.md __init__.py brand_blocks.py data_loaders.py lp_orchestrator.py mega_prompt_builder.py repair_loop.py
```

### Architecture map regen

```bash
python3 scripts/update_architecture_map.py
git diff .claude/docs/state/WEBAPP_ARCHITECTURE_MAP.yaml | grep "gsg_lp" | head -10
# Expected: lines showing growthcro/gsg_lp/ entries REMOVED (lines starting with "-")
```

### Capabilities check

```bash
python3 scripts/audit_capabilities.py 2>&1 | grep -E "Orphans HIGH|Partial"
# Expected: Orphans HIGH : 0 / Partial wired : 0
```

## Dependencies

### Externes
- Aucune

### Internes
- État main `ff586be` (post Epic 1)
- Worktree : à créer (CCPM convention)
- `scripts/update_architecture_map.py` (idempotent)
- `scripts/audit_capabilities.py` (orphan detector)

### Sequencing
- Parallel safe avec 001 et 003

## Effort Estimate
- Size: S
- Hours: 1

## Definition of Done
- [ ] Pré-check pass (0 import actif)
- [ ] Move exécuté via `git mv` (préserve history)
- [ ] Architecture map regénérée, gsg_lp absent
- [ ] Capabilities orphans HIGH=0
- [ ] Gates verts
- [ ] Commit isolé conforme convention
