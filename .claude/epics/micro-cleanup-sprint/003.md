---
name: Relocate _archive_deprecated + .gitignore pattern
status: open
created: 2026-05-12T13:30:00Z
updated: 2026-05-12T13:30:00Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Relocate _archive_deprecated + .gitignore pattern

## Description

Déplacer `skills/site-capture/scripts/_archive_deprecated_2026-04-19/` (viole anti-pattern #10 archive dans active path) vers `_archive/skills_site-capture_deprecated_2026-04-19_root_relocate_2026-05-12/`. Ajouter une entry `.gitignore` préventive `**/_archive_deprecated_*/` pour empêcher la réintroduction. Cette task fait passer **lint FAIL=1 → 0** et clear le 1 mypy error résiduel (duplicate module `score_site`).

## Acceptance Criteria

- [ ] `skills/site-capture/scripts/_archive_deprecated_2026-04-19/` n'existe plus dans active tree
- [ ] `_archive/skills_site-capture_deprecated_2026-04-19_root_relocate_2026-05-12/` contient tout le contenu d'origine
- [ ] `.gitignore` contient la ligne `**/_archive_deprecated_*/` (et ses justifications en commentaire)
- [ ] `python3 scripts/lint_code_hygiene.py` retourne **FAIL=0** (de 1 à 0)
- [ ] `python3 -m mypy growthcro/ moteur_gsg/ moteur_multi_judge/ skills/` retourne **0 error** (de 1 à 0, le duplicate `score_site` disparaît)
- [ ] `bash scripts/typecheck.sh` exit 0 (strict 0 + global maintenant 0)
- [ ] `bash scripts/parity_check.sh weglot` exit 0 (108 files baseline)
- [ ] `python3 SCHEMA/validate_all.py` exit 0
- [ ] `python3 scripts/audit_capabilities.py` orphans HIGH=0
- [ ] Pré-check : aucun script référence le chemin `_archive_deprecated_2026-04-19/` hors du dossier lui-même
- [ ] Commit isolé : `chore(cleanup): relocate _archive_deprecated_2026-04-19 hors active path + .gitignore guard (#<N>)`

## Technical Details

### Pré-check référence path

```bash
cd /Users/mathisfronty/Developer/epic-micro-cleanup-sprint

# Statique
grep -rn "_archive_deprecated_2026-04-19" \
  growthcro/ moteur_gsg/ moteur_multi_judge/ skills/ scripts/ 2>&1 \
  | grep -v "Binary file\|^_archive\|^skills/site-capture/scripts/_archive_deprecated" \
  | head -20

# Dynamique (strings)
grep -rn "'_archive_deprecated_2026-04-19'\|\"_archive_deprecated_2026-04-19\"" \
  growthcro/ moteur_gsg/ moteur_multi_judge/ skills/ scripts/ 2>&1 \
  | grep -v "^_archive" | head -10
```

Si match trouvé → noter dans progress.md ("Path reference found at <file:line>, may need update post-move") et procéder quand même (move + sed-replace les references).

### Move command

```bash
# Verify source exists
ls skills/site-capture/scripts/_archive_deprecated_2026-04-19/ 2>&1 | head -3

# Verify destination doesn't exist
ls _archive/skills_site-capture_deprecated_2026-04-19_root_relocate_2026-05-12/ 2>&1 | head -3

# Move
git mv skills/site-capture/scripts/_archive_deprecated_2026-04-19/ \
       _archive/skills_site-capture_deprecated_2026-04-19_root_relocate_2026-05-12/

# Verify
ls _archive/skills_site-capture_deprecated_2026-04-19_root_relocate_2026-05-12/ | head -10
```

### .gitignore entry

Read `.gitignore` first. Check si la ligne existe déjà :
```bash
grep -n "_archive_deprecated" .gitignore 2>&1 | head -5
```

Si la ligne `**/_archive_deprecated_*/` n'est pas exactement présente (les 2 matches existants peuvent être différents patterns), ajouter à la fin du fichier (avant les sections "macOS" ou autres OS-specific) :

```gitignore

# Anti-pattern #10 prevention (CLAUDE.md) — _archive_deprecated_* doit vivre sous _archive/ racine,
# jamais dans un active path. Linter custom flagge ce drift en FAIL.
**/_archive_deprecated_*/
```

### Verification finale

```bash
# Lint must show FAIL=0
python3 scripts/lint_code_hygiene.py 2>&1 | head -5

# mypy must show 0 errors
python3 -m mypy growthcro/ moteur_gsg/ moteur_multi_judge/ skills/ 2>&1 | tail -3

# typecheck gate
bash scripts/typecheck.sh
```

## Dependencies

### Externes
- Aucune

### Internes
- État main `ff586be` (post Epic 1)
- Worktree : à créer (CCPM convention)
- `scripts/lint_code_hygiene.py` (gate à passer FAIL=0)
- `scripts/typecheck.sh` (Epic 1 gate)

### Sequencing
- Parallel safe avec 001 et 002

## Effort Estimate
- Size: XS
- Hours: 0.5

## Definition of Done
- [ ] Pré-check références (aucun script ne dépend du path source)
- [ ] Move exécuté via `git mv` (préserve history)
- [ ] `.gitignore` entry ajoutée (commentaire + pattern)
- [ ] Lint FAIL=0 confirmé
- [ ] mypy 0 error confirmé
- [ ] Gates verts
- [ ] Commit isolé conforme convention
