---
name: Creative Exploration Engine — schema + orchestrator + persist + CLI
status: open
created: 2026-05-16T18:35:00Z
updated: 2026-05-16T18:35:00Z
github: TBD
depends_on: []
parallel: true
conflicts_with: []
wave: 1
effort: L
hours: 16
---

# Task CR-01 — Creative Exploration Engine

## Description

Créer le package `moteur_gsg/creative_engine/` qui appelle Claude Opus 4.7 pour générer 3-5 directions créatives audacieuses pour une LP donnée. Sortie structurée Pydantic v2, pas de production HTML.

Source : PRD §FR-1 + Codex addendum §"Proposed New Layer: Creative Exploration Engine".

## Acceptance Criteria

- [ ] Package `moteur_gsg/creative_engine/` créé (mono-concern, 4 axes : validation + orchestration + persistence + CLI)
- [ ] `growthcro/models/creative_models.py` NEW : `CreativeRoute`, `CreativeRouteBatch`, `RouteThesis` (Pydantic v2 strict frozen+extra=forbid, ≤200 LOC)
  - `CreativeRoute` fields : `route_id`, `route_name`, `aesthetic_thesis`, `spatial_layout_thesis`, `hero_mechanism`, `section_rhythm`, `visual_metaphor`, `motion_language`, `texture_language`, `image_asset_strategy`, `typography_strategy`, `color_strategy`, `proof_visualization_strategy`, `page_type_modules: list[str]`, `risks: list[str]`, `why_this_route_fits: str`
  - `CreativeRouteBatch` : `client_slug`, `page_type`, `business_category`, `routes: list[CreativeRoute] (min_length=3, max_length=5)`, `prompt_meta`
- [ ] `moteur_gsg/creative_engine/orchestrator.py` : appel Opus 4.7 avec system prompt structuré + user message (brief + brand_dna + design_grammar + page_type + business_category). Parse output JSON → CreativeRouteBatch via Pydantic. Retry once si parse fail (Sonnet self-correction si Opus output malformed).
- [ ] `moteur_gsg/creative_engine/persist.py` : atomic write `data/captures/<client>/<page_type>/creative_routes.json` (pattern même que `growthcro/opportunities/persist.py`)
- [ ] `moteur_gsg/creative_engine/cli.py` : `python3 -m growthcro.creative_engine.cli explore --client <slug> --page <page_type>` exit 0/1
- [ ] `moteur_gsg/creative_engine/__init__.py` re-exports
- [ ] `tests/creative_engine/` tests pytest :
  - `test_schema.py` : Pydantic rejette routes <3 ou >5, rejette risks vide, rejette why_this_route_fits trop court (<20 chars), frozen mutation rejected
  - `test_persist.py` : round-trip save/load
  - `test_orchestrator.py` (with mocked Opus call) : parse output → 5 routes
  - `test_cli.py` : CLI exit 0 sur weglot/lp_listicle (mocked)

## Technical Details

**Fichiers créés (~6 source + 4 test + fixtures)** :
- `moteur_gsg/creative_engine/__init__.py` (~30 LOC)
- `moteur_gsg/creative_engine/orchestrator.py` (~200 LOC, axe orchestration)
- `moteur_gsg/creative_engine/persist.py` (~80 LOC, axe persistence)
- `moteur_gsg/creative_engine/cli.py` (~80 LOC, axe CLI)
- `growthcro/models/creative_models.py` (~180 LOC, axe TYPING)
- `tests/creative_engine/__init__.py`
- `tests/creative_engine/test_schema.py` (~120 LOC)
- `tests/creative_engine/test_persist.py` (~80 LOC)
- `tests/creative_engine/test_orchestrator.py` (~150 LOC mocked)
- `tests/creative_engine/test_cli.py` (~60 LOC)
- `tests/creative_engine/fixtures/weglot_lp_listicle_routes.json` (sample output)

**Anti-mega-prompt** : le system prompt Opus DOIT être <8K chars (CLAUDE.md #1). Structure :
```
Section 1 : context (brief + brand_dna + design_grammar) — max 4K chars
Section 2 : task spec (génère 3-5 routes JSON valid) — max 1K chars
Section 3 : output schema (JSON example) — max 2K chars
Section 4 : constraints (anti-AI-slop banned defaults, mais toolbox open) — max 1K chars
Total : ≤8K chars hard limit, validate at module load via assert
```

**Model** : `claude-opus-4-7` (cf. décisions tranchées epic.md). Pas de fallback Sonnet (Sonnet est pour copy, pas exploration).

**Coût attendu** : ~$0.5-1 par run de 5 routes (input ~5K tokens, output ~8K tokens × 5 routes).

## Stop Conditions

13 standard CLAUDE.md. Particuliers :
- Si Opus output not JSON parseable AFTER 1 retry → log error, raise CreativeEngineError, don't fake routes
- Si CreativeRoute schema needs change → DRAFT D-only, ask Mathis before implementing
- Si prompt dépasse 8K chars → STOP refactor avant d'envoyer (anti-pattern #1)

## Out of Scope

- HTML production (= CR-04 Visual Composer)
- Route selection (= CR-02 Visual Judge)
- Multi-provider abstraction (1 Opus suffit per décision Mathis)
- Image gen (= CR-04)
- Brief V2 schema extension (validate if existing suffit en CR-01, sinon issue séparée)

## Dependencies

- ✅ Anthropic Opus 4.7 API access (ANTHROPIC_API_KEY déjà OK)
- ✅ `growthcro/lib/anthropic_client.py` existe et utilisable
- Cet issue déclenche : CR-02 (Visual Judge consomme CreativeRouteBatch) + CR-03 (Contracts schemas)
