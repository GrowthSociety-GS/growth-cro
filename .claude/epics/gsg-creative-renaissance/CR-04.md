---
name: Visual Composer + vocabulary library multi-vertical
status: open
created: 2026-05-16T18:35:00Z
updated: 2026-05-16T18:35:00Z
github: TBD
depends_on: [CR-02, CR-03]
parallel: false
conflicts_with: []
wave: 2
effort: XL
hours: 24
---

# Task CR-04 — Visual Composer

## Description

Package `moteur_gsg/visual_composer/` qui transforme `CreativeRouteContract` (CR-03) + vocabulary library multi-vertical → `VisualComposerContract` concret (sections + modules + motion + asset specs). Génère aussi les prompts pour image gen (DALL-E 3).

Source : PRD §FR-4 + Codex addendum §"Visual Composer".

## Acceptance Criteria

- [ ] Package `moteur_gsg/visual_composer/` créé
- [ ] Vocabulary library JSON `moteur_gsg/visual_composer/vocabulary/` :
  - `saas.json` (B2B SaaS pattern : dashboard mockups, founder-led narrative, ROI graphs)
  - `ecommerce.json` (PDP-style, product cards, urgency, badges)
  - `luxury.json` (minimal, large negative space, serif typography, B/W photography)
  - `marketplace.json` (multi-supply, social proof high density, search-first hero)
  - `app_acquisition.json` (mobile-first, app store badges, video previews)
  - `media_editorial.json` (long-form, editorial typography, byline, citations)
  - `local_service.json` (trust signals, contact prominent, before-after, testimonials geo)
  - `health_wellness.json` (calming palette, science-backed proof, transformations)
  - `finance.json` (trust + compliance, numbers prominent, security badges)
  - `creator_course.json` (instructor-led, curriculum preview, social proof learners)
  - `enterprise.json` (logos tier 1, security/SOC2 prominent, case studies depth)
  - `consumer_brand.json` (lifestyle, emotional, large hero imagery)
  - Each file : `hero_mechanisms[]`, `section_rhythms[]`, `visual_metaphors[]`, `motion_patterns[]`, `texture_rules[]`, `image_strategies[]`, `typography_scales[]` (5-15 items per category)
- [ ] `moteur_gsg/visual_composer/composer.py` (axe orchestration, ≤250 LOC) :
  - `compose(route_contract: CreativeRouteContract, business_category: str, brief: dict) -> VisualComposerContract`
  - Lookup vocabulary[business_category]
  - Match route.thesis fields to vocabulary items (greedy + Sonnet fallback if ambiguous)
  - Generate concrete section specs + modules + motion + assets
- [ ] `moteur_gsg/visual_composer/asset_brief_generator.py` (axe prompt_assembly, ≤150 LOC) :
  - Pour chaque `AssetSpec` dans VisualComposerContract, draft un `prompt_for_image_gen` ciblé DALL-E 3
  - Inputs : asset.asset_type + route.aesthetic_thesis + brand_dna.color_palette + brand_dna.tone
  - Output : prompt string optimisé DALL-E (style + composition + mood + technical specs)
- [ ] `moteur_gsg/visual_composer/motion_specs_generator.py` (axe I/O serialization, ≤120 LOC) :
  - Convertit `motion_language` thesis en `MotionSpec` concrets (CSS keyframes ou JS animation hints)
- [ ] `growthcro/lib/image_client.py` NEW (axe API client, ≤150 LOC) :
  - Wrapper DALL-E 3 via OpenAI SDK
  - Lazy import `openai` (optional dep)
  - `generate_image(prompt: str, size: str = "1024x1024", quality: str = "standard") -> Path` (download to `data/captures/<client>/<page_type>/assets/`)
  - Read `OPENAI_API_KEY` from `growthcro.config`
- [ ] Update `growthcro/config.py` : `OPENAI_API_KEY` reste optional (déjà schema-listed) MAIS add config helper `openai_api_key()` qui raise EnvError si missing (similar pattern to anthropic)
- [ ] Add `openai>=1.40` to `requirements.txt` (optional install via extras)
- [ ] Tests pytest :
  - `test_composer.py` : 3 routes différentes × 3 verticals → 9 VisualComposerContract valides
  - `test_asset_brief_generator.py` : 1 asset_spec → prompt string non-vide ≥50 chars
  - `test_motion_specs_generator.py` : 1 motion_language → ≥1 MotionSpec
  - `test_image_client.py` : mocked OpenAI client, 1 image generated path correct

## Technical Details

**Fichiers créés (~5 source + 3 test + 12 vocabulary JSON)** :
- `moteur_gsg/visual_composer/__init__.py` (~30 LOC)
- `moteur_gsg/visual_composer/composer.py` (~240 LOC)
- `moteur_gsg/visual_composer/asset_brief_generator.py` (~140 LOC)
- `moteur_gsg/visual_composer/motion_specs_generator.py` (~110 LOC)
- `moteur_gsg/visual_composer/vocabulary/*.json` (12 files, ~150 LOC chacun = ~1800 LOC total)
- `growthcro/lib/image_client.py` (~140 LOC)
- `tests/visual_composer/` : 4 test files (~400 LOC total)

**Coût image gen** : DALL-E 3 standard quality 1024x1024 = $0.04/image. HD = $0.08. Per LP : ~3-8 images = $0.12-0.64.

**SECURITY** : OPENAI_API_KEY rotation discipline. Ajout `.env.example` mention.

## Stop Conditions

13 standard. Particuliers :
- Si LOC `composer.py` dépasse 250 → split en `composer.py` (orchestration) + `_vocabulary_matcher.py` (matching logic)
- Si OpenAI API down → fail loud, don't fake images (no placeholder bullshit)
- Si DALL-E refuse certains prompts (content policy) → log warning, skip asset, continue

## Out of Scope

- Real image gen runs in tests (mocked)
- Multi-provider image (Midjourney/FLUX = v2)
- Vocabulary self-learning depuis runs réussis (= Learning Layer V29 future)
- Mode `--ship-strict` integration (= CR-08)

## Dependencies

- CR-02 (Visual Judge selects route → CreativeRouteContract)
- CR-03 (VisualComposerContract schema)
- OPENAI_API_KEY added to `.env` by Mathis avant le start
- `openai>=1.40` pip install
