---
name: 5 custom skills Renaissance + CLAUDE.md step #13 update + wire mode_1
status: open
created: 2026-05-16T18:35:00Z
updated: 2026-05-16T18:35:00Z
github: TBD
depends_on: [CR-06, CR-07]
parallel: true
conflicts_with: []
wave: 3
effort: M
hours: 12
---

# Task CR-08 — 5 custom skills + final wire

## Description

Créer 5 skills custom invocables via Skill tool (mono-concern, anti-mega-prompt), update CLAUDE.md step #13 pour les mentionner, wire le pipeline Renaissance dans `mode_1_complete.py` (post-impeccable, pre-multi-judge).

Source : PRD §FR-8 + Codex addendum §"Skills / Tools Direction".

## Acceptance Criteria

### 5 skills custom

- [ ] `.claude/skills/gsg-creative-explorer/SKILL.md` créé
  - Frontmatter : `name`, `description: trigger "explore creative routes", "3-5 directions", "creative exploration"`
  - Body : when to use (avant generate HTML), call sequence (creative_engine.cli explore), output (CreativeRouteBatch JSON)
- [ ] `.claude/skills/gsg-visual-judge/SKILL.md` créé
  - Trigger : "select best route", "score routes", "judge creative"
  - Body : when, call (creative_engine.judge), output (RouteSelectionDecision JSON)
- [ ] `.claude/skills/gsg-visual-composer/SKILL.md` créé
  - Trigger : "compose visual", "section rhythm", "visual composer"
  - Body : when, call (visual_composer.compose), output (VisualComposerContract JSON)
- [ ] `.claude/skills/gsg-brand-fit-judge/SKILL.md` créé
  - Trigger : "brand alignment check", "brand fit score", "is this on-brand?"
  - Body : when, call (creative_engine.judge avec brand_fit-only mode OR séparé skill)
- [ ] `.claude/skills/gsg-renderer-qa/SKILL.md` créé
  - Trigger : "screenshot review", "visual QA", "review rendered LP"
  - Body : when, call (renderer_qa.screenshot_capture + screenshot_inspector), output (ScreenshotQAReport JSON)
- [ ] Chaque skill : ≤200 LOC body, frontmatter Anthropic conforme, mono-concern (1 step pipeline, no mega-prompt)
- [ ] Skills detected via Skill tool en nouvelle session (verify in skills list)

### CLAUDE.md step #13 update

- [ ] Update `.claude/CLAUDE.md` step #13 :
  - Mention `gsg-creative-explorer`, `gsg-visual-judge`, `gsg-visual-composer`, `gsg-brand-fit-judge`, `gsg-renderer-qa` (en plus des 3 skills existants growthcro-anti-drift / prd-planner / status-memory)
  - Add Creative Renaissance pipeline reference : "Avant tout GSG generate, déclencher creative_explorer → visual_judge → visual_composer → renderer → renderer_qa pour qualité premium multi-vertical"

### Wire dans mode_1_complete

- [ ] `moteur_gsg/modes/mode_1_complete.py` modifié (+30 LOC max) :
  - Ajouter import : `from moteur_gsg.creative_engine import explore_routes, select_route, CreativeRouteContract`
  - Ajouter import : `from moteur_gsg.visual_composer import compose, VisualComposerContract`
  - Ajouter import : `from moteur_gsg.renderer_qa import capture_dual_viewport, inspect_screenshots`
  - Pipeline étape 4.5 (entre context_pack et current generation) :
    ```python
    if creative_renaissance_enabled:  # feature flag, default True post-CR-08
        routes_batch = explore_routes(brief, brand_dna, design_grammar, page_type, business_category)
        route_decision = select_route(routes_batch, brief, brand_dna)
        composer_contract = compose(route_decision.selected_route_contract, business_category, brief)
    ```
  - Pipeline étape 7.5 (entre HTML save et impeccable) :
    ```python
    if creative_renaissance_enabled and composer_contract:
        screenshots = capture_dual_viewport(html_path, output_dir)
        qa_report = inspect_screenshots(screenshots, route_decision.selected_route_contract)
        gen["screenshot_qa_report"] = qa_report.model_dump()
        if ship_strict and not qa_report.gate_passed:
            raise ScreenshotQAError(qa_report)
    ```
- [ ] Backward compat : si `creative_renaissance_enabled=False` (env var ou flag), pipeline legacy continue de marcher
- [ ] Feature flag : `growthcro/config.py` add `RENAISSANCE_ENABLED` (default True)
- [ ] Manifest §12 bump : entry Sprint Renaissance close

### Tests + smoke

- [ ] Tests pytest : 1 test par skill mention dans skills list (5 tests trivial frontmatter validation)
- [ ] `tests/gsg/test_renaissance_pipeline_e2e.py` : full pipeline end-to-end mocked (no real LLM/API calls), verify all 6 Renaissance modules invoked in order
- [ ] Smoke heavy : Weglot lp_listicle run avec Renaissance enabled (Mathis manual, post-merge)

## Technical Details

**Fichiers créés (~5 skills + tests)** :
- `.claude/skills/gsg-creative-explorer/SKILL.md`
- `.claude/skills/gsg-visual-judge/SKILL.md`
- `.claude/skills/gsg-visual-composer/SKILL.md`
- `.claude/skills/gsg-brand-fit-judge/SKILL.md`
- `.claude/skills/gsg-renderer-qa/SKILL.md`
- `tests/gsg/test_renaissance_pipeline_e2e.py` (~150 LOC mocked)

**Fichiers modifiés (~3)** :
- `.claude/CLAUDE.md` (+10 LOC step #13 update)
- `moteur_gsg/modes/mode_1_complete.py` (+30 LOC pipeline wire — careful LOC déjà à 1040)
- `growthcro/config.py` (+5 LOC RENAISSANCE_ENABLED feature flag)
- `.claude/docs/reference/GROWTHCRO_MANIFEST.md` §12 (changelog entry Renaissance complete, commit séparé)

**Anti-pattern** : Chaque skill ≤200 LOC. Pas de mega-prompt. Pas de logique métier dans le skill (juste descriptors + when-to-use + invocation hints).

## Stop Conditions

13 standard. Particuliers :
- Si mode_1_complete.py LOC dépasse 1100 post-wire → STOP, split obligatoire (Wave 3 #50/#51 a déjà bumpé à 1040, +30 = 1070, borderline)
- Si feature flag `RENAISSANCE_ENABLED` casse legacy tests → debug avant ship, no regression Wave 3 baseline
- Si manifest §12 a déjà une entry Renaissance (race condition) → merge proprement, don't overwrite

## Out of Scope

- Webapp UI pour browse creative routes (= séparé frontend)
- Real-time A/B test routes en prod (= Experiment Engine V27 future)
- Skills training/calibration data collection (= Learning Layer V29 future)
- Anti-pattern detection automated dans CI (= P1+ séparé)

## Dependencies

- CR-06 (Renderer extension done = pipeline runnable)
- CR-07 (Promptfoo eval suite = non-regression proof)
- Mathis post-merge smoke validation
