---
name: Update Sidebar + delete microfrontends.json + update workspace scripts
status: open
created: 2026-05-13T09:42:00Z
updated: 2026-05-13T09:42:00Z
github: (no GitHub sync)
depends_on: ["001", "002"]
parallel: false
conflicts_with: []
---

# Task 003: Update Sidebar + delete microfrontends.json + update workspace scripts

## Description

Mettre à jour la navigation Sidebar pour pointer vers les nouvelles routes pluriel `/audits` et `/recos`. Supprimer le fichier de config microfrontends Vercel (`microfrontends.json`). Retirer les scripts npm workspace dev pour les microfrontends archivés. Retirer le bloc `rewrites()` localhost dans `next.config.js`.

## Acceptance Criteria

- [ ] `webapp/apps/shell/components/Sidebar.tsx` : items array updated
  - `{label:"Audit", href:"/audit", hint:"185 pages"}` → `{label:"Audits", href:"/audits", hint:"185 pages"}`
  - `{label:"Recos", href:"/reco", hint:"3045 items"}` → `{label:"Recos", href:"/recos", hint:"3045 items"}`
- [ ] `webapp/apps/shell/app/page.tsx` (Home) : liens cross-feature updated
  - `<a href="/audit" className="gc-btn">Audit</a>` → `<a href="/audits" className="gc-btn">Audits</a>`
  - `<a href={`/audit/${c.slug}`}>` (Top clients section) → `<a href={`/audits/${c.slug}`}>`
  - `<a href="/audit" className="gc-pill gc-pill--soft">Tout voir</a>` → `<a href="/audits" className="gc-pill gc-pill--soft">Tout voir</a>`
- [ ] `webapp/apps/shell/next.config.js` : retirer le bloc `async rewrites()` complètement
- [ ] `webapp/microfrontends.json` : supprimer (rm via git)
- [ ] `webapp/package.json` : retirer scripts `dev:audit`, `dev:reco`, `dev:gsg`, `dev:reality`, `dev:learning`
- [ ] `webapp/vercel.json` : INCHANGÉ (build apps/shell already correct)

## Technical Details

### Sidebar.tsx target state

```tsx
const ITEMS: { label: string; href: string; hint: string }[] = [
  { label: "Overview", href: "/", hint: "Pipeline" },
  { label: "Audits", href: "/audits", hint: "185 pages" },
  { label: "Recos", href: "/recos", hint: "3045 items" },
  { label: "GSG Studio", href: "/gsg", hint: "Brief + LP" },
  { label: "Audit Google Ads", href: "/audit-gads", hint: "Agency" },
  { label: "Audit Meta Ads", href: "/audit-meta", hint: "Agency" },
  { label: "Reality", href: "/reality", hint: "Soon" },
  { label: "Learning", href: "/learning", hint: "V29/V30" },
];
```

### next.config.js target state

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ["@growthcro/ui", "@growthcro/data", "@growthcro/config"],
  experimental: {
    typedRoutes: false,
  },
};

module.exports = nextConfig;
```

(retirer `const path = require("path")` aussi si plus utilisé)

### package.json target state (webapp/)

Garder seulement :
- `dev` (= shell)
- `dev:shell`
- `build`, `lint`, `typecheck`, `test:e2e`

Retirer : `dev:audit`, `dev:reco`, `dev:gsg`, `dev:reality`, `dev:learning`

### Sidebar active state

L'expression `pathname.startsWith(`${it.href}/`)` continue à fonctionner avec `/audits` (match `/audits/<slug>`) et `/recos` (match `/recos/<slug>`).

### Liens cross-feature à updater (grep validation)

Après cette task :
```bash
grep -r 'href="/audit"' webapp/apps/shell  # 0 results (sauf /audit-gads et /audit-meta)
grep -r 'href="/audit/' webapp/apps/shell  # 0 results (sauf /audit-gads, /audit-meta)
grep -r 'href="/reco"' webapp/apps/shell    # 0 results
grep -r 'href="/reco/' webapp/apps/shell   # 0 results
```

## Dependencies

- Task 001 (audits/ et recos/ pages existent)
- Task 002 (gsg/, reality/, learning/ pages existent)

## Effort Estimate

- Size: XS
- Time: 10 min

## Definition of Done

- [ ] Sidebar updated avec pluriel
- [ ] Home page liens updated
- [ ] next.config.js rewrites retirés
- [ ] microfrontends.json supprimé
- [ ] package.json scripts dev:* retirés
- [ ] vercel.json INCHANGÉ
