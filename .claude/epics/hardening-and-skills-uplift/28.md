---
name: Observability Migration (growthcro/observability/logger.py + top-10 pipelines print → logger)
status: closed
created: 2026-05-12T07:16:27Z
updated: 2026-05-12T07:31:54Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/28
depends_on: [25]
parallel: false
conflicts_with: []
---

# Task: Observability Migration

## Description

Créer module observability stdlib + migrer top-10 pipelines `print()` → `logger` structuré. Promouvoir règle linter `INFO[print-in-pipeline]` → `WARN`. Foundation pour POC Logfire/Axiom futur.

Référence : [`reports/CODE_AUDIT_2026-05-11.md`](../../../reports/CODE_AUDIT_2026-05-11.md) §3.5, §6 action #6.

## Acceptance Criteria

- [ ] **Module créé** : `growthcro/observability/__init__.py` + `growthcro/observability/logger.py` (≤200 LOC, mono-concern)
- [ ] **API publique stable** :
  ```python
  from growthcro.observability.logger import get_logger
  logger = get_logger(__name__)
  logger.info("...", extra={"correlation_id": ..., "client": ...})
  ```
- [ ] **Caractéristiques** :
  - Lecture log level via `growthcro/config.py` (nouvelle var env `GROWTHCRO_LOG_LEVEL`, défaut `INFO`)
  - Format structuré JSON (compatibles Logfire/Axiom/Sentry futurs)
  - Correlation ID auto-générée par invocation pipeline (`contextvars.ContextVar`)
  - Stdout par défaut (preserve subprocess parsing downstream)
  - Pas de dépendance externe (stdlib only)
- [ ] **Migration top-10 pipelines** (300 prints converted total) :
  | Fichier | Prints | Action |
  |---|---:|---|
  | `growthcro/cli/capture_full.py` | 75 | `logger.info` |
  | `growthcro/capture/orchestrator.py` | 34 | `logger.info` + correlation_id |
  | `moteur_gsg/modes/mode_1/orchestrator.py` | 33 | idem |
  | `growthcro/gsg_lp/lp_orchestrator.py` | 33 | idem (legacy mais top offender) |
  | `growthcro/capture/scorer.py` | 26 | idem |
  | `moteur_gsg/modes/mode_1_complete.py` | 20 | idem |
  | `moteur_gsg/core/pipeline_sequential.py` | 19 | idem |
  | `moteur_gsg/core/pipeline_single_pass.py` | 17 | idem |
  | `growthcro/cli/enrich_client.py` | 17 | CLI |
  | `moteur_multi_judge/orchestrator.py` | 16 | idem |
- [ ] **Linter promu** : règle `INFO[print-in-pipeline]` → `WARN` dans `scripts/lint_code_hygiene.py`. Re-baseliner les ~157 prints restants (CLIs OK, scripts utilitaires OK).
- [ ] **Doctrine étendue** : `docs/doctrine/CODE_DOCTRINE.md` nouveau §LOG (pattern logger structuré, exemple, anti-patterns).
- [ ] **WEBAPP_ARCHITECTURE_MAP.yaml** : nouveau module `growthcro/observability/logger.py` ajouté (purpose, inputs, outputs, depends_on, lifecycle_phase: infrastructure).
- [ ] **Gates verts** :
  - `python3 scripts/lint_code_hygiene.py` exit 0 (FAIL ≤ 1 = local junk inchangé). Vérifier WARN inclut les 157 prints CLI/utilitaires restants.
  - `python3 scripts/audit_capabilities.py` orphans = 0
  - `python3 SCHEMA/validate_all.py` exit 0
  - `bash scripts/parity_check.sh weglot` exit 0 — **CRITIQUE** : la migration doit préserver le comportement stdout pour subprocess capture downstream
  - `bash scripts/agent_smoke_test.sh` exit 0
  - 6/6 GSG checks PASS
- [ ] MANIFEST §12 changelog entry (commit séparé).

## Technical Details

- Pré-requis : Task #1 mergée (linter FAIL = 0 avant promotion règle)
- Pas de Logfire/Axiom intégration dans ce sprint (foundation only) — POC séparé futur
- Module `logger.py` design :
  ```python
  import json, logging, sys, uuid
  from contextvars import ContextVar
  from growthcro.config import config
  
  _correlation_id: ContextVar[str] = ContextVar("correlation_id", default="")
  
  class StructuredFormatter(logging.Formatter):
      def format(self, record):
          payload = {
              "ts": self.formatTime(record),
              "level": record.levelname,
              "logger": record.name,
              "msg": record.getMessage(),
              "correlation_id": _correlation_id.get() or None,
              **(getattr(record, "extra_fields", None) or {}),
          }
          return json.dumps(payload, default=str)
  
  def get_logger(name: str) -> logging.Logger:
      ...
  
  def set_correlation_id(value: str | None = None) -> str:
      val = value or uuid.uuid4().hex[:12]
      _correlation_id.set(val)
      return val
  ```
- Migration print → logger : commits granulaires (1 commit par fichier = 10 commits + 1 module + 1 linter + 1 doctrine + 1 manifest = 14 total)
- Worktree dédié : `task-4-observability`
- Subprocess parsing downstream : tester avec un run pipeline e2e qui parse stdout (s'il existe) ou document risk dans stream-A

## Dependencies

- Task #1 (Hygiene Quick-Wins) — linter doit passer à FAIL = 0 avant promotion règle INFO → WARN

## Effort Estimate

- Size: M
- Hours: 24-40 (4-5 jours focus)

## Definition of Done

- [ ] `growthcro/observability/logger.py` créé ≤200 LOC
- [ ] 300 prints migrés dans top-10 pipelines
- [ ] Règle linter `print-in-pipeline` promue WARN
- [ ] `CODE_DOCTRINE.md` §LOG ajouté
- [ ] WEBAPP_ARCHITECTURE_MAP.yaml mis à jour
- [ ] Gates verts (parity ✓ CRITIQUE pour preserve stdout)
- [ ] MANIFEST §12 entry
- [ ] Mathis : "observability foundation prête, je peux intégrer Logfire/Sentry SDK plus tard"
