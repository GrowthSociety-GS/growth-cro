---
name: Reorganize root-level Python entrypoints
status: closed
created: 2026-05-08T11:14:20Z
updated: 2026-05-09T11:04:58Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/9
depends_on: [3, 5]
parallel: true
conflicts_with: [5]
---

# Task: Reorganize root-level Python entrypoints

## Description

Move the remaining root-level Python files into the `growthcro/` package. After this task, the repo root contains only the conventional anchors (`README.md`, `CLAUDE.md`, `AGENTS.md`, `Dockerfile`, `docker-compose.yml`, `.gitignore`, `requirements.txt`, `package.json`, `package-lock.json`, `.env.example`, `state.py`).

**Files to relocate:**
- `api_server.py` (11 KB) → `growthcro/api/server.py`
- `add_client.py` (13 KB) → `growthcro/cli/add_client.py`
- `capture_full.py` (24 KB) → `growthcro/cli/capture_full.py`
- `enrich_client.py` (25 KB) → `growthcro/cli/enrich_client.py`

(`ghost_capture_cloud.py` is owned by Task 004 — already moved into `growthcro/capture/` by then. `state.py` stays at root by convention.)

Each old root path becomes a shim. `Dockerfile` and `docker-compose.yml` updated to use module-style entrypoints (`python -m growthcro.api.server` etc.).

## Acceptance Criteria

- [ ] `growthcro/api/__init__.py` and `growthcro/api/server.py` exist; FastAPI app instance importable via `from growthcro.api.server import app`.
- [ ] `growthcro/cli/__init__.py` and the three CLI modules exist with clean argparse entrypoints.
- [ ] `api_server.py`, `add_client.py`, `capture_full.py`, `enrich_client.py` at root → 5–10 line shims.
- [ ] If any of the 4 root files exceed 800 LOC after relocation, split them in this task following the concern-axis approach (note: `enrich_client.py` is 25KB ≈ likely ~700 LOC — verify; if >800, split).
- [ ] `Dockerfile` `CMD` updated to `python -m growthcro.api.server` (or equivalent).
- [ ] `docker-compose.yml` services updated.
- [ ] `package.json` `"scripts"` updated if any reference root paths.
- [ ] `run_enricher.sh` updated.
- [ ] `parity_check.sh weglot` passes.
- [ ] `python -m growthcro.api.server` boots locally; `/health` endpoint responds 200.
- [ ] `docker compose up` (smoke) succeeds on the new entrypoints. (Optional if Mathis hasn't built locally — at minimum `docker compose config` validates.)
- [ ] Capabilities-keeper green.

## Technical Details

- The conflict with Task 004 is real: both touch root-level files. Resolution: 004 lands first (handles `ghost_capture_cloud.py`), then 008 starts. Hence `depends_on: [002, 004]`.
- Shim pattern is identical to Task 4. Example for `api_server.py`:
  ```python
  # api_server.py — shim, removed in Task 10
  from growthcro.api.server import app  # noqa
  if __name__ == "__main__":
      import uvicorn
      uvicorn.run(app, host="0.0.0.0", port=8000)
  ```
- Verify all sub-agents that may shell out to these entrypoints (look in `.claude/agents/*.md`) — if any hardcode `python api_server.py`, the shim covers them; agent definitions are refreshed in Task 9.
- Docker note: if `Dockerfile` `COPY` lines list specific files, update to `COPY growthcro/ growthcro/`.

## Dependencies

- Task 002 (config module).
- Task 004 (handles root-level `ghost_capture_cloud.py` first, avoiding merge conflict).
- Conflicts with Task 004 — sequenced via `depends_on`.

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done

- [ ] Root contains only the agreed anchors
- [ ] All 4 root files moved and shimmed
- [ ] Docker entrypoints updated and tested
- [ ] `parity_check.sh weglot` passes
- [ ] Capabilities-keeper green
- [ ] Mathis approves
