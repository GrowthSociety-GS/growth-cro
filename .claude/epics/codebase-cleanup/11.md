---
name: Remove shims, regenerate manifests, final parity
status: closed
created: 2026-05-08T11:14:20Z
updated: 2026-05-09T11:04:58Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/11
depends_on: [10]
parallel: false
conflicts_with: []
---

# Task: Remove shims, regenerate manifests, final parity

## Description

Wave-3 closing task #2. With all sub-agents and external callers now pointing at canonical paths (Task 009), the shim wrappers from Wave 2 are dead weight. Remove them. Regenerate the project-wide documentation (`CAPABILITIES_SUMMARY.md`, subpackage `README.md`s, `MANIFEST §12`). Run the final parity check.

## Acceptance Criteria

- [ ] All shim wrappers from Tasks 004, 005, 006, 007, 008 deleted. Old paths no longer exist.
- [ ] `find .` shows no file at the legacy locations (`ghost_capture_cloud.py`, `api_server.py`, `add_client.py`, `capture_full.py`, `enrich_client.py`, `skills/site-capture/scripts/perception_v13.py`, etc.).
- [ ] `CAPABILITIES_REGISTRY.json` regenerated; orphan count remains 0.
- [ ] `docs/state/CAPABILITIES_SUMMARY.md` regenerated from the new tree.
- [ ] Each subpackage has a `README.md` (≤30 lines):
  - `growthcro/README.md` — the package overview
  - `growthcro/config.py` (no README needed; the docstring suffices)
  - `growthcro/lib/README.md`
  - `growthcro/capture/README.md`
  - `growthcro/perception/README.md`
  - `growthcro/scoring/README.md`
  - `growthcro/recos/README.md`
  - `growthcro/api/README.md`
  - `growthcro/cli/README.md`
  - `moteur_gsg/README.md` updated to reflect the post-split structure
- [ ] Each README lists: purpose (1 line), entrypoint(s), upstream/downstream dependencies in the import graph.
- [ ] `docs/reference/GROWTHCRO_MANIFEST.md` §12 has a new dated changelog entry (`YYYY-MM-DD — codebase-cleanup epic landed: ...`).
- [ ] `tree -L 3 -I 'node_modules|_archive|__pycache__|data|deliverables|worktrees'` fits on one screen and reads like the architecture (per PRD US-3).
- [ ] `parity_check.sh weglot` passes byte-for-byte against the Task-1 baseline.
- [ ] Parity passes on 2 random additional clients.
- [ ] All success metrics from the PRD's Success Criteria table reach their targets:
  - Active source files ≤140
  - Active LOC ≤55,000 (or ≤60,000 if dedup yield was lower)
  - Files >800 LOC = 0
  - Root-level `.py` = 1 (`state.py`)
  - Basename duplicates (active) = 0
  - `os.environ` reads (active) = 1 file
  - Orphans = 0
  - Archive folders inside active = 0
- [ ] Capabilities-keeper green.

## Technical Details

- Shim removal is mechanical: `git rm <path>` for each.
- Verify no caller still references the shim before deletion. The Task-9 agent smoke test and the Task-9 sub-agent audit should have caught them all; this task re-runs both as a final sanity check.
- The `MANIFEST §12` entry is a single dated paragraph following the existing format; commit it separately per `CLAUDE.md` rule ("commit séparé : `docs: manifest §12 — add YYYY-MM-DD changelog for <change>`").
- The subpackage READMEs are short on purpose (≤30 lines). Their job is "tell an LLM cold-reading where to look", not be a full reference.

## Dependencies

- Task 009.

## Effort Estimate

- Size: S
- Hours: 4

## Definition of Done

- [ ] All shims removed
- [ ] Documentation regenerated
- [ ] All PRD success metrics met
- [ ] `parity_check.sh` passes on 3 clients
- [ ] `MANIFEST §12` entry committed separately
- [ ] Capabilities-keeper green
- [ ] Mathis sign-off on the final tree before merging `epic/codebase-cleanup` to `main`
