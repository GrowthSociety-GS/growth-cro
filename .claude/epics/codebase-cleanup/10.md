---
name: Wire orphans, kill remaining duplicates, refresh sub-agents
status: closed
created: 2026-05-08T11:14:20Z
updated: 2026-05-09T11:04:58Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/10
depends_on: [4, 5, 6, 7, 8, 9]
parallel: false
conflicts_with: []
---

# Task: Wire orphans, kill remaining duplicates, refresh sub-agents

## Description

Wave-3 closing task #1. After all Wave-2 splits land, drive the codebase to its target state: zero orphans, zero basename duplicates in active paths, zero sub-agents pointing at stale paths. This is the cleanup *of* the cleanup.

## Acceptance Criteria

- [ ] `python3 scripts/audit_capabilities.py` → `potentially_orphaned: 0` for active paths.
  - Every previously-orphaned file is either: (a) imported by an active entrypoint (wired), or (b) moved to `_archive/`.
  - For each (b) decision, post a one-line rationale in `_archive/README.md` (e.g. "removed: superseded by `growthcro/recos/orchestrator.py` since 2026-04-29").
- [ ] `find . -name "*.py" -not -path "*/_archive/*" -not -path "*/__pycache__/*" -not -path "*/node_modules/*" -not -path "*/worktrees/*" | xargs -n1 basename | sort | uniq -d` returns nothing (zero basename duplicates in active paths). Note: `__init__.py` and `cli.py` are exempt — they're expected to repeat, one per package; filter them.
- [ ] Every `.claude/agents/*.md` audited:
  - All file paths referenced exist
  - All shell commands resolve (Python module paths importable, scripts executable)
  - Updated to point at new canonical paths (NOT shims — agents reference the new module-style paths)
- [ ] A new `scripts/agent_smoke_test.sh` created — runs each agent's first command in `--help` / dry-run mode where possible, asserts exit 0 or expected error code.
- [ ] `parity_check.sh weglot` passes byte-for-byte against the Task-1 baseline.
- [ ] Run parity on 2 additional clients picked at random (suggested: pull 2 from `data/clients_database.json` excluding `weglot`).
- [ ] Capabilities-keeper sub-agent invoked at task end; reports OK with the regenerated registry.

## Technical Details

- The orphan list is the deliverable from `audit_capabilities.py`. Walk it; each row is a decision: wire or archive.
- Wiring usually means adding an `import` statement in the right `__init__.py` or upstream module. Sometimes it's a CLI registration.
- Sub-agents to audit (current inventory from `.claude/agents/`):
  - `capabilities-keeper`
  - `capture-worker`
  - `claude-code-guide`
  - `doctrine-keeper`
  - `Explore`
  - `general-purpose`
  - `Plan`
  - `reco-enricher`
  - `scorer`
  - `statusline-setup`
  - (any others — list with `ls .claude/agents/`)
- For each agent, check: file path references, slash-command syntax, expected output paths, environment expectations.
- The smoke test script is plain bash. It's not a unit test — it just calls each agent's referenced commands with `--help` (or invokes the dry-run path) and confirms they exist and don't ImportError.

## Dependencies

- All Wave-2 tasks (003 through 008).

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done

- [ ] Zero orphans in active paths
- [ ] Zero basename duplicates (excluding `__init__.py`, `cli.py`)
- [ ] All sub-agents audited and updated
- [ ] `agent_smoke_test.sh` passes
- [ ] Parity passes on 3 clients
- [ ] Capabilities-keeper green
