---
name: Split perception & scoring god files
status: closed
created: 2026-05-08T11:14:20Z
updated: 2026-05-09T11:04:58Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/7
depends_on: [3]
parallel: true
conflicts_with: []
---

# Task: Split perception & scoring god files

## Description

The middle of the pipeline (perception → scoring) lives in three god files. Refactor along the natural concern boundaries: heuristic perception vs vision-based perception vs intent detection vs persistence on the perception side; pillar-level scoring vs page-type scoring vs UX-specific scoring on the scoring side.

**Targets:**
- `skills/site-capture/scripts/perception_v13.py` (1,134 LOC)
- `skills/site-capture/scripts/score_specific_criteria.py` (1,101 LOC)
- `skills/site-capture/scripts/score_ux.py` (710 LOC) — under 800 but currently mixed-concern
- (consider) `scripts/site_intelligence.py` (1,070 LOC) if it's part of the perception flow

## Acceptance Criteria

- [ ] `growthcro/perception/` package created with:
  - `heuristics.py` — rule-based perception (DOM-driven)
  - `vision.py` — vision-model calls (Sonnet vision / OpenAI / etc.)
  - `intent.py` — intent detection logic
  - `persist.py` — write perception JSON
  - `cli.py` — argparse entrypoint
- [ ] `growthcro/scoring/` package created with:
  - `pillars.py` — 6-pillar scoring (matches `playbook/bloc_*_v3.json` structure)
  - `page_type.py` — page-type-specific scoring
  - `specific_criteria.py` — what's left of `score_specific_criteria.py` after extraction
  - `ux.py` — UX-specific scoring
  - `persist.py` — write score JSON
  - `cli.py` — argparse entrypoint
- [ ] No file >800 LOC in either package.
- [ ] One-line concern docstring in each file.
- [ ] Old paths (`perception_v13.py`, `score_specific_criteria.py`, `score_ux.py`) become shims.
- [ ] If `site_intelligence.py` is part of perception, fold it in; otherwise document why it's separate.
- [ ] All env reads via `growthcro.config`.
- [ ] `parity_check.sh weglot` passes byte-for-byte for perception JSON and score JSON.
- [ ] Re-running batch scoring on the fleet (524 score artefacts per `state.py`) is idempotent.
- [ ] Capabilities-keeper green.

## Technical Details

- The scoring side is doctrine-driven: every pillar maps to `playbook/bloc_<N>_*_v3.json`. Use that mapping to define the file boundaries on the scoring side — `pillars.py` reads the playbook, dispatches to per-pillar logic. Do NOT change the playbook contents (out of scope; doctrine-keeper territory).
- The perception side splits cleanly when you look at the existing class/function names — there are usually clear `heuristic_*`, `vision_*`, `intent_*` clusters.
- `score_ux.py` at 710 LOC may pass the 800 hard cap but be mixed-concern. Apply the rule: if it's >300 LOC AND has divergent function-name prefixes, refactor.
- Post the file map for both packages as a single issue comment before committing.

## Dependencies

- Task 002.
- Disjoint sub-tree from capture/recos/GSG; parallelizable with 004, 005, 007.

## Effort Estimate

- Size: L
- Hours: 14

## Definition of Done

- [ ] Both packages created with single-concern modules
- [ ] No file >800 LOC
- [ ] Shims at old paths
- [ ] `parity_check.sh weglot` passes
- [ ] Capabilities-keeper green
- [ ] Mathis approves the file map
