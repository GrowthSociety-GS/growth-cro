---
name: Doctrine TYPING + Manifest + Architecture map regen
status: open
created: 2026-05-12T10:11:22Z
updated: 2026-05-12T10:11:22Z
github: (will be set on sync)
depends_on: [004]
parallel: false
conflicts_with: []
---

# Task: Doctrine TYPING + Manifest + Architecture map regen

## Description

Ajouter une section `§TYPING` à `docs/doctrine/CODE_DOCTRINE.md` (règle : modèles Pydantic obligatoires à toute frontière inter-module). Inscrire le changelog dans `docs/reference/GROWTHCRO_MANIFEST.md` §12 (commit séparé per CLAUDE.md rule). Régénérer l'architecture map via `scripts/update_architecture_map.py` (idempotent).

## Acceptance Criteria

- [ ] `.claude/docs/doctrine/CODE_DOCTRINE.md` édité — nouvelle section `§TYPING` ajoutée
- [ ] La règle §TYPING énonce : "Tout fichier à frontière inter-module (consommé par ≥2 modules) DOIT exposer Pydantic v2 BaseModel en signatures publiques. `dict[str, Any]` interdit en frontière inter-module. Validation `ConfigDict(extra='forbid')` par défaut."
- [ ] Mention du gate `scripts/typecheck.sh` et de la config `pyproject.toml` overrides
- [ ] `.claude/docs/reference/GROWTHCRO_MANIFEST.md` §12 — entry changelog ajoutée :
  - Date du jour
  - Titre : "Typing Strict Rollout"
  - Référence à l'epic + 3 fichiers top-coupling
  - Baseline mypy avant/après mesurée (88 → N)
- [ ] **Commit MANIFEST séparé** (per CLAUDE.md rule : `docs: manifest §12 — add YYYY-MM-DD changelog for Typing Strict Rollout`)
- [ ] `python3 scripts/update_architecture_map.py` exécuté (idempotent) — sortie `WEBAPP_ARCHITECTURE_MAP.yaml` régénérée
- [ ] Architecture map intègre `growthcro/models/` package + ses 3 modules
- [ ] Vue humaine `WEBAPP_ARCHITECTURE_MAP.md` Mermaid regénérée
- [ ] `python3 scripts/audit_capabilities.py` orphans HIGH=0 (les 3 nouveaux modèles wired indirectement via leurs consommateurs)
- [ ] `python3 scripts/lint_code_hygiene.py` exit 0
- [ ] PRD `typing-strict-rollout.md` frontmatter mis à jour : `status: completed`, `updated: <ISO>`
- [ ] Epic `typing-strict-rollout/epic.md` frontmatter mis à jour : `status: completed`, `progress: 100%`
- [ ] Commit séparé doctrine : `docs: doctrine §TYPING + epic typing-strict-rollout completed`

## Technical Details

### CODE_DOCTRINE.md §TYPING (template)

```markdown
## §TYPING — Modèles Pydantic à frontière inter-module (depuis 2026-05-XX)

**Règle** : tout fichier dont la sortie publique est consommée par ≥2 modules différents DOIT exposer cette sortie via un modèle Pydantic v2 (`pydantic.BaseModel`).

**Pourquoi** : (1) validation runtime à la frontière catche les schema drifts comme `ValidationError` explicites ; (2) `model_json_schema()` permet l'interop TS pour la webapp V28 ; (3) typage strict mypy possible sur les fichiers à blast radius élevé.

**Comment** :
- Modèles dans `growthcro/models/<topic>_models.py` (mono-concern, ≤200 LOC)
- `model_config = ConfigDict(extra='forbid', frozen=True)` par défaut
- `frozen=False` exceptionnel + justification commentaire
- `dict[str, Any]` interdit en signature publique ; toléré en escape-hatch interne uniquement, et à typer dans un sprint follow-up

**Gate** : `scripts/typecheck.sh` doit exit 0 sur scope strict (`pyproject.toml` `[[tool.mypy.overrides]]`).

**Scope initial (typing-strict-rollout, 2026-05-XX)** :
- `moteur_gsg/core/visual_intelligence.py` → `growthcro/models/visual_models.py`
- `moteur_gsg/core/context_pack.py` → `growthcro/models/context_models.py`
- `growthcro/recos/orchestrator.py` → `growthcro/models/recos_models.py`

**Extension future** : top 5 suivants par coupling (capture/orchestrator, mode_1/orchestrator, multi_judge/orchestrator, gsg_lp/lp_orchestrator, scorer) — sprints follow-up dédiés.
```

### MANIFEST §12 changelog entry

```markdown
### 2026-05-XX — Typing Strict Rollout

**Epic** : `typing-strict-rollout` (Wave A du PRD master `post-stratosphere-roadmap`)

**Livré** :
- 3 modules Pydantic v2 mono-concern dans `growthcro/models/` :
  - `visual_models.py` (VisualBlock, VisualHierarchy, VisualScore, VisualReport)
  - `context_models.py` (ContextPackInput, ContextPackOutput, PageContext, ClientContext)
  - `recos_models.py` (RecoInput, RecoEnriched, RecoBatch, EvidenceLedgerEntry)
- 3 fichiers top-coupling refactorisés (visual_intelligence.py, context_pack.py, recos/orchestrator.py)
- 5 callsites mis à jour (scorer, mode_1/orchestrator, multi_judge/orchestrator, persona_narrator [V26.AF préservé], enrich_client)
- `pyproject.toml` overrides `mypy --strict` sur top-3 + `growthcro.models.*`
- `scripts/typecheck.sh` gate exit 1 si régression
- Doctrine §TYPING ajoutée (règle modèles Pydantic à frontière inter-module)

**Métriques** :
- mypy global : **88 → N errors** (-37% absorbés)
- mypy --strict sur scope ciblé : exit 0
- 0 régression V26.AF (persona prompt ≤8K chars assert préservé)
- 0 régression V3.2.1 / V3.3 (playbooks intacts)
- 0 régression parity weglot (108 files baseline)
- 6/6 GSG checks PASS
```

### Architecture map regen

```bash
cd /Users/mathisfronty/Developer/growth-cro
python3 scripts/update_architecture_map.py
git diff .claude/docs/state/WEBAPP_ARCHITECTURE_MAP.yaml | head -50
# Vérifier que les 3 nouveaux modules apparaissent dans le YAML
```

## Dependencies

### Externes
- Aucune

### Internes
- 004 closed (mypy gate active, baseline mesurée)
- 001 + 002 + 003 closed (modèles + refactor + callsites)
- `scripts/update_architecture_map.py` existant + idempotent (déjà validé Sprint #28)

### Sequencing
- Strictement séquentiel après 004
- **Dernière task de l'epic** — clôture officielle

## Effort Estimate
- Size: S
- Hours: 2

## Definition of Done
- [ ] CODE_DOCTRINE.md §TYPING ajouté
- [ ] MANIFEST §12 changelog ajouté avec métriques réelles mesurées en Task 004
- [ ] Commit MANIFEST séparé (CLAUDE.md rule)
- [ ] Architecture map regénérée (YAML + MD Mermaid)
- [ ] PRD + epic frontmatter status: completed
- [ ] Gates verts : lint + parity + schemas + capabilities-keeper + smoke
- [ ] Commit doctrine isolé conforme convention
- [ ] Epic progress : 5/5 tasks closed → 100%
