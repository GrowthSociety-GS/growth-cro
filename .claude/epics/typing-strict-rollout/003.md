---
name: Pydantic-ize recos/orchestrator
status: open
created: 2026-05-12T10:11:22Z
updated: 2026-05-12T10:11:22Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Pydantic-ize recos/orchestrator

## Description

Créer `growthcro/models/recos_models.py` (Pydantic v2, ≤200 LOC, mono-concern) puis refactor `growthcro/recos/orchestrator.py` (610 LOC, 10 mypy errors args optional / returns hétérogènes) pour exposer `RecoBatch` en signature publique. Mettre à jour le callsite consommateur `growthcro/cli/enrich_client.py`.

## Acceptance Criteria

- [ ] `growthcro/models/recos_models.py` créé ≤ 200 LOC
- [ ] Modèles exposés : `RecoInput`, `RecoEnriched`, `RecoBatch`, `EvidenceLedgerEntry`
- [ ] `model_config = ConfigDict(extra='forbid', frozen=True)` par défaut
- [ ] `orchestrate_recos(input: RecoInput) -> RecoBatch` signature publique
- [ ] Args optional explicitement `Optional[...] = None` (pas de `dict | None` ambigu)
- [ ] `mypy --strict growthcro/recos/orchestrator.py growthcro/models/recos_models.py` exit 0
- [ ] 10 errors absorbées sur `recos/orchestrator.py`
- [ ] Round-trip JSON validé in-task sur fichier réel `data/recos/<slug>/<page_type>/recos_enriched.json`
- [ ] Callsite `growthcro/cli/enrich_client.py` mis à jour pour consommer `RecoBatch`
- [ ] Evidence ledger V26.A préservé (chaque `RecoEnriched` a un `evidence_ids: list[str]` non-empty)
- [ ] `python3 scripts/lint_code_hygiene.py` exit 0
- [ ] `bash scripts/parity_check.sh weglot` exit 0
- [ ] `python3 SCHEMA/validate_all.py` exit 0
- [ ] Commit isolé : `feat(typing): Pydantic-ize recos/orchestrator (#<issue_num>)`

## Technical Details

### Modèles à concevoir (recos_models.py)

```python
class EvidenceLedgerEntry(BaseModel):
    """V26.A traçabilité reco → evidence (capture id, criterion id, perception cluster)."""
    model_config = ConfigDict(extra='forbid', frozen=True)
    evidence_id: str
    source_type: Literal["capture", "perception", "scoring", "doctrine"]
    criterion_id: str | None = None
    cluster_id: str | None = None
    excerpt: str | None = None

class RecoInput(BaseModel):
    model_config = ConfigDict(extra='forbid', frozen=True)
    slug: str
    page_type: str
    capture_path: str
    perception_path: str
    scoring_path: str
    doctrine_version: Literal["v3.2.1", "v3.3"] = "v3.2.1"
    options: dict[str, Any] = Field(default_factory=dict)

class RecoEnriched(BaseModel):
    """Reco enrichie via Sonnet 4.5 (reco_enricher_v13_api.py)."""
    model_config = ConfigDict(extra='forbid', frozen=True)
    reco_id: str
    pillar: Literal["hero", "persuasion", "ux", "coherence", "psycho", "tech", "utility"]
    criterion_id: str
    severity: Literal["critical", "high", "medium", "low"]
    title: str
    description: str
    rationale: str
    evidence_ids: list[str]  # V26.A non-empty
    proposed_action: str

class RecoBatch(BaseModel):
    """Output public de recos/orchestrator — batch de recos enrichies pour 1 page."""
    model_config = ConfigDict(extra='forbid', frozen=True)
    slug: str
    page_type: str
    recos: list[RecoEnriched]
    evidence_ledger: list[EvidenceLedgerEntry]
    enrichment_meta: dict[str, Any] = Field(default_factory=dict)  # llm cost, tokens, latency
```

### Refactor recos/orchestrator.py

- Identifier les 10 errors via `mypy growthcro/recos/orchestrator.py` baseline (capture output dans commit body)
- Pattern : returns `dict | list | None` ambigus → `RecoBatch` (Optional géré explicitement)
- Args optional : `enrich(slug, page_type, options=None)` → `enrich(input: RecoInput)` ou `enrich(slug: str, page_type: str, *, options: Optional[dict] = None)`
- **610 LOC** : si refactor pousse au-delà 800 LOC (limit doctrine), split en sub-modules mono-concern (`orchestrator.py` + `enrichment.py` + `evidence.py`) — discovery in-task.

### Callsite enrich_client.py

```python
# Avant :
batch = orchestrate_recos(slug, page_type, options={...})
for r in batch["recos"]:
    print(r["title"])

# Après :
batch: RecoBatch = orchestrate_recos(RecoInput(slug=slug, page_type=page_type, capture_path=..., ...))
for r in batch.recos:
    print(r.title)  # ou logger.info(r.title) — observability §LOG
```

### Round-trip test

```bash
python3 -c "
from growthcro.models.recos_models import RecoBatch
import json
with open('data/recos/weglot/lp_listicle/recos_enriched.json') as f:
    batch = RecoBatch.model_validate(json.load(f))
print(f'{len(batch.recos)} recos, {len(batch.evidence_ledger)} evidence entries')
"
```

## Dependencies

### Externes (techniques)
- Pydantic v2
- mypy 1.x

### Internes
- État main `9a79076`
- Fichier `data/recos/<slug>/<page_type>/recos_enriched.json` accessible (56 clients × 185 pages disponibles)
- Evidence ledger V26.A spec ([reference dans la doctrine code])
- `growthcro/config.py`

### Sequencing
- Parallel safe avec 001 et 002
- Bloque 004
- **Si splitting nécessaire** (610 LOC + refactor potentially → >800) : split en sub-task discoverable in-sprint, commit isolé

## Effort Estimate
- Size: M
- Hours: 12

## Definition of Done
- [ ] Code implemented (recos_models.py + recos/orchestrator.py refactor + enrich_client.py callsite)
- [ ] Evidence ledger V26.A invariant préservé (evidence_ids non-empty pour chaque reco)
- [ ] Round-trip JSON validé in-task sur fichier réel
- [ ] Gates verts : lint + parity + schemas + 6/6 GSG checks + smoke
- [ ] `mypy --strict growthcro/recos/orchestrator.py growthcro/models/recos_models.py` exit 0
- [ ] Si splitting nécessaire (>800 LOC) : sub-modules mono-concern + capabilities-keeper validation
- [ ] 0 régression V3.2.1 / V3.3
- [ ] Commit isolé conforme convention `Issue #<N>: <description>`
