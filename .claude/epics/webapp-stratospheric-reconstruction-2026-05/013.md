---
name: global-chrome-cmdk-breadcrumbs
status: in-progress
created: 2026-05-13T17:12:51Z
updated: 2026-05-15T11:00:00Z
github: (will be set on sync)
depends_on: [001, 002, 003, 004, 005, 006, 007, 008, 009, 010, 011, 012]
parallel: false
conflicts_with: []
tier: 4
effort_days: 2
priority: P2
---

# Task 013 : global-chrome-cmdk-breadcrumbs

## Description

Chrome global aligné V26 : sticky header avec search bar + Cmd+K command palette + 11-item sidebar avec count badges + breadcrumbs dynamiques. Source : V26 HTML L860-898 (`<aside class="sidebar">` + `<header class="header">`).

Cette task touche TOUS les routes — donc execute en fin pour ne pas conflicter avec les tasks précédentes.

## Acceptance Criteria

- [ ] **Sticky header** (`<StickyHeader />` NEW) :
  - Position fixed top
  - Backdrop blur + gold border bottom
  - Contains : breadcrumbs + search + actions (notifications, profile)
- [ ] **Cmd+K command palette** (`<CmdKPalette />` NEW) :
  - Library : `cmdk` (~6KB) OR custom (zero new dep)
  - Open on Cmd+K (or Ctrl+K Windows)
  - Items : navigate to client (51) · navigate to audit · navigate to setting · trigger action (Run audit / Add client) · open doctrine pilier
  - Fuzzy search across all items
  - Recent items section (localStorage)
- [ ] **11-item sidebar avec count badges** (per V26 L866-877) :
  - Dashboard
  - 🎯 Growth Audit + badge (audits count)
  - 🔄 Scent Trail
  - 🧬 Brand DNA + badge (clients with DNA count)
  - 📐 Design Grammar
  - 👁️ Reality Layer + V26.C badge
  - 🧪 Experiments + V27 badge
  - 🤖 GEO + V31 badge
  - 🧠 Learning
  - 📘 Doctrine
  - ⚙️ Settings
- [ ] **Breadcrumbs dynamiques** (`<DynamicBreadcrumbs />` NEW) :
  - Derive from `usePathname()` + custom segment labels (e.g. `/audits/weglot/home` → "Audits / Weglot / Home")
  - Last segment bold + clickable up the chain
- [ ] **Search bar in header** (replaces existing search if any) :
  - `/` shortcut to focus search (like architecture-explorer.html L300)
  - Triggers same palette as Cmd+K but inline
- [ ] **Active route highlighting** : sidebar item gold accent on active route

## Technical Details

### Files créés
- `webapp/apps/shell/components/chrome/StickyHeader.tsx` (NEW)
- `webapp/apps/shell/components/chrome/CmdKPalette.tsx` (NEW)
- `webapp/apps/shell/components/chrome/DynamicBreadcrumbs.tsx` (NEW)
- `webapp/apps/shell/components/chrome/SidebarNavBadge.tsx` (NEW)
- `webapp/apps/shell/lib/cmdk-items.ts` (NEW, items registry)
- `webapp/apps/shell/lib/use-keyboard-shortcuts.ts` (NEW hook)

### Files modifiés
- `webapp/apps/shell/components/Sidebar.tsx` (refactor : add badges + active state + 11 items)
- `webapp/apps/shell/components/Breadcrumbs.tsx` (deprecate, use DynamicBreadcrumbs)
- `webapp/apps/shell/components/ViewToolbar.tsx` (deprecate or integrate into StickyHeader)
- `webapp/apps/shell/app/layout.tsx` (mount StickyHeader + CmdKPalette globally)

### Cmd+K items registry
```typescript
// cmdk-items.ts
export const cmdkItems: CmdKItem[] = [
  // Navigation
  { id: 'nav-home', label: 'Dashboard', kind: 'nav', href: '/' },
  { id: 'nav-clients', label: 'Clients', kind: 'nav', href: '/clients' },
  // ... 11 sidebar items
  // Actions
  { id: 'act-add-client', label: 'Add new client', kind: 'action', onClick: () => openAddClientModal() },
  { id: 'act-run-audit', label: 'Run audit', kind: 'action', onClick: () => openRunAuditModal() },
  // Client navigations (dynamic, fetched at render)
  // Audit navigations (dynamic)
];
```

## Dependencies

- **Code** : ALL previous tasks (001-012) need to be merged first because chrome touches every route
- **External** : optional `cmdk` npm package (~6KB), or implement custom (zero new dep)

## Effort Estimate
- **Size**: M
- **Hours**: 12-16 (2 jours solo dev)

Breakdown :
- StickyHeader + backdrop : 2h
- DynamicBreadcrumbs : 2h
- CmdKPalette (custom or with cmdk lib) : 4h
- 11-sidebar refactor + badges fetching : 3h
- Active route highlighting + animations : 1h
- Playwright E2E : 2h
- Cross-route smoke test : 1h

## Definition of Done

- [x] Code implemented per acceptance criteria (7 NEW files + 4 modified, 3 commits via parallel-agent worktree)
- [x] Cmd+K palette implemented in 357 LOC zero-new-dep (React state + createPortal + substring filter — `cmdk` package banned per "no new dep")
- [x] All 10 sidebar items visible (GEO disabled-greyed pending Task 009 ship) + 4 count badges (clients/audits/recosP0/learning-deferred)
- [x] Breadcrumbs render correctly via DynamicBreadcrumbs (hidden on /, /login, /privacy, /terms ; UUID detector truncates to 8 chars)
- [x] Typecheck shell exit 0
- [x] ESLint shell exit 0
- [x] **`npm run build --workspace=apps/shell` exit 0** — 27/27 pages generated (no node:fs client-bundle issue)
- [x] Code hygiene gate exit 0
- [x] Playwright contract spec written (`webapp/tests/e2e/global-chrome.spec.ts`)
- [x] Cumulative regression suite **190/190 PASS prod** (zero drift across 11 sprints)
- [x] PR merged + Vercel deploy sha=`f78b439` on https://growth-cro.vercel.app
- [ ] Mathis manual validation : "Chrome global V26 restauré + Cmd+K productif" — Cmd+K opens palette · fuzzy filter works · nav items navigate correctly · breadcrumbs render correctly across routes · sidebar badges visible

## Implementation log — 2026-05-15

**Files créés (7)**
- `webapp/apps/shell/components/chrome/StickyHeader.tsx` — sticky-top + backdrop blur + gold border-bottom + breadcrumbs (left) + search (center) + actions (right)
- `webapp/apps/shell/components/chrome/CmdKPalette.tsx` — 357 LOC zero-new-dep palette (React state + createPortal + substring filter), Cmd+K trigger, recent items localStorage `gc-cmdk-recent`, a11y `role="dialog"` + ESC + focus restore
- `webapp/apps/shell/components/chrome/DynamicBreadcrumbs.tsx` — derives from `usePathname()`, extended SEGMENT_LABELS map for scent/experiments/handoff/geo/funnel/judges/dna, UUID detector truncates to 8 chars
- `webapp/apps/shell/components/chrome/SidebarNavBadge.tsx` — small count badge with defensive null/0 hiding
- `webapp/apps/shell/lib/cmdk-items.ts` — single source of truth for the 11 nav entries (NAV_ENTRIES), consumed by both Sidebar + palette so they can't drift
- `webapp/apps/shell/lib/use-keyboard-shortcuts.ts` — pure hook, document-level listener (Cmd+K opens palette, `/` focuses search), gates on `e.metaKey || e.ctrlKey` to not trap focus in inputs
- `webapp/tests/e2e/global-chrome.spec.ts` — Playwright contract spec

**Files modifiés (4)**
- `webapp/apps/shell/components/Sidebar.tsx` — rewired to consume the shared NAV_ENTRIES registry + sidebar badges + gold accent active state
- `webapp/apps/shell/components/Breadcrumbs.tsx` — deprecated, re-exports DynamicBreadcrumbs (ViewToolbar callsite unchanged)
- `webapp/apps/shell/app/page.tsx` — mounts StickyHeader + passes sidebarBadges prop {clients, audits, recosP0, learning:null}
- `webapp/apps/shell/app/globals.css` — chrome CSS block

**Décisions notables (agent worktree)**
- Cmd+K palette : zero new dep — React state + `createPortal` from `react-dom` + plain substring `.includes()` filter in `cmdk-items.ts` (~30 LOC for the filter). Palette = 357 LOC single-concern
- GEO entry kept in NAV_ENTRIES registry with `disabled: true` — Sidebar renders it as a static greyed row with `title="Task 009 — coming soon"`, palette filters it out (no keyboard navigation to a 404)
- Sidebar badge wiring : home page passes `{clients: count, audits: sum_of_audits_count, recosP0: metrics.recosP0, learning: null}` — `learning` deferred (file-based proposals would need a new loader)
- Breadcrumb segments : extended SEGMENT_LABELS (+ scent/experiments/handoff/geo/funnel/judges/dna) + UUID detector truncates to 8 chars ; hidden on root routes `/`, `/login`, `/privacy`, `/terms`
- Shared registry pattern : `lib/cmdk-items.ts` as single source of truth prevents Sidebar + palette drift
- Per-page topbar overlap : home keeps the legacy `<CommandCenterTopbar />` alongside the new `<StickyHeader />` to stay strictly within Task 013 scope ; cleanup of per-page topbars across all routes is a non-blocking follow-up Mathis can decide on
