---
name: gsg-design-grammar-viewer-restore
status: in-progress
created: 2026-05-13T17:12:51Z
updated: 2026-05-15T09:00:00Z
github: (will be set on sync)
depends_on: [002]
parallel: false
conflicts_with: []
tier: 3
effort_days: 2
priority: P1
---

# Task 010 : gsg-design-grammar-viewer-restore

## Description

Fix the misplacement de `/gsg` route per Décision D3.A : actuellement `/gsg` = Brief Wizard mort (Studio.tsx orphelin), alors que la V26 surface canonique `/gsg` = Design Grammar viewer (7 artefacts/client). Cette task déplace le Brief Wizard à `/gsg/handoff` (en le wirant correctement à task 002 pipeline-trigger-backend) et rebuilds `/gsg` comme Design Grammar viewer.

## Acceptance Criteria

- [ ] **Move current `/gsg`** content → `/gsg/handoff` :
  - `webapp/apps/shell/app/gsg/page.tsx` → `webapp/apps/shell/app/gsg/handoff/page.tsx`
  - Import `BriefWizard` from `components/gsg/BriefWizard.tsx` (currently orphan)
  - Wire `<TriggerRunButton type="gsg" />` (from task 002) at end of wizard → calls `/api/runs/gsg`
- [ ] **Rebuild `/gsg`** route as Design Grammar viewer per V26 L3067-3248 :
  - Sidebar layout : client selector (51 panel clients)
  - On client select : render 7 artefacts grid
- [ ] **7 Design Grammar artefacts** (`<DesignGrammarViewer />` NEW) per client :
  - `tokens.css` preview (rendered live in iframe)
  - `tokens.json` JSON tree
  - `component_grammar.json` (rules per component type)
  - `section_grammar.json` (rules per section)
  - `composition_rules.json` (do/dont)
  - `brand_forbidden_patterns.json` (warning UI)
  - `quality_gates.json` (gates per pattern)
- [ ] **Forbidden patterns warning UI** (`<ForbiddenPatternsAlert />` NEW) :
  - Lists each forbidden pattern with rationale
  - Visual examples (if `forbidden_examples` images on disk)
- [ ] **Live tokens.css preview** (`<TokensCssPreview />` NEW) :
  - Renders the tokens.css in a sandboxed iframe
  - Shows a sample card using the tokens (dogfood demo)
- [ ] **Preview iframe live GSG output** (post task 002) :
  - When `/gsg/handoff` triggers a GSG run via task 002
  - `<GsgRunPreview />` (NEW) subscribes to Supabase Realtime `runs` filtered by run_id
  - On completion, fetch generated HTML from Supabase Storage → render in sandboxed iframe

## Technical Details

### Files créés
- `webapp/apps/shell/app/gsg/page.tsx` (REWRITE as DG viewer)
- `webapp/apps/shell/app/gsg/handoff/page.tsx` (NEW, BriefWizard relocated)
- `webapp/apps/shell/components/gsg/DesignGrammarViewer.tsx` (NEW)
- `webapp/apps/shell/components/gsg/TokensCssPreview.tsx` (NEW)
- `webapp/apps/shell/components/gsg/ForbiddenPatternsAlert.tsx` (NEW)
- `webapp/apps/shell/components/gsg/GsgRunPreview.tsx` (NEW)
- `webapp/apps/shell/lib/design-grammar-fs.ts` (NEW data layer)

### Files modifiés
- `webapp/apps/shell/components/Sidebar.tsx` : "📐 Design Grammar" nav-item (currently no nav-item for /gsg cleanly)
- `webapp/apps/shell/lib/gsg-api.ts` : update import paths if BriefWizard moves
- `webapp/apps/shell/components/gsg/BriefWizard.tsx` : wire TriggerRunButton at submit

### Disk source
`data/captures/<client>/design_grammar/` contains 7 files :
- `tokens.css`
- `tokens.json`
- `component_grammar.json`
- `section_grammar.json`
- `composition_rules.json`
- `brand_forbidden_patterns.json`
- `quality_gates.json`

Either load from disk OR migrate to Supabase storage. **Recommended** : add to migration script (task 001 of phase migration follow-up) to upload these artefacts to Supabase Storage `design_grammar` bucket. For now, serve via `/api/design-grammar/[client]/[file]` route that redirects to Supabase Storage URL (like SP-11 screenshots pattern).

## Dependencies

- **Code** : task 002 (pipeline-trigger-backend) — BriefWizard needs TriggerRunButton wired
- **Disk** : `data/captures/<client>/design_grammar/*` (51/56 panel clients have it per fleet stats)

## Effort Estimate
- **Size**: M
- **Hours**: 12-16 (2 jours solo dev)

Breakdown :
- Move /gsg → /gsg/handoff : 1h
- Wire BriefWizard + TriggerRunButton : 2h
- DesignGrammarViewer + 7-artefact grid : 4h
- TokensCssPreview iframe : 2h
- ForbiddenPatternsAlert : 1h
- GsgRunPreview with Realtime : 2h
- API route `/api/design-grammar/...` : 2h
- Playwright E2E : 2h

## Definition of Done

- [x] Code implemented per acceptance criteria (10 NEW files + 3 modified, 4 commits via parallel-agent worktree)
- [x] Typecheck shell exit 0
- [x] ESLint shell exit 0
- [x] **`npm run build --workspace=apps/shell` exit 0** — route tree includes `/gsg` (4.18 kB) + `/gsg/handoff` (5.37 kB) + `/api/design-grammar/[client]/[file]` (dynamic)
- [x] Code hygiene gate exit 0
- [x] Playwright contract spec written + PASS prod (route never-500 + /login mount + anonymous never sees admin)
- [x] Cumulative regression suite 182/182 PASS prod (zero drift across 10 sprints)
- [x] PR merged + Vercel deploy sha=`2ff15fb` on https://growth-cro.vercel.app
- [ ] Mathis manual validation : "/gsg restauré V26 + /gsg/handoff fonctionnel" — `/gsg` shows Design Grammar viewer with client selector + 7-artefact grid · TokensCssPreview iframe renders sandboxed · ForbiddenPatternsAlert visible if data dispo · `/gsg/handoff` shows BriefWizard + `<TriggerRunButton type="gsg">` at submit step

## Implementation log — 2026-05-15

**Files créés (10)**
- `webapp/apps/shell/lib/design-grammar-types.ts` — pure types + helpers shared client/server (zero Node imports)
- `webapp/apps/shell/lib/design-grammar-fs.ts` — server-only data layer with `import "server-only";` guard, reads `data/captures/<client>/design_grammar/*` 7 files
- `webapp/apps/shell/app/api/design-grammar/[client]/[file]/route.ts` — mirrors SP-11 screenshots pattern (302 to Supabase Storage when `NEXT_PUBLIC_SUPABASE_URL` set, fs fallback for local dev)
- `webapp/apps/shell/app/gsg/handoff/page.tsx` — Brief Wizard relocated, admin-gated
- `webapp/apps/shell/components/gsg/DesignGrammarViewer.tsx` — 7-artefact grid Server Component
- `webapp/apps/shell/components/gsg/TokensCssPreview.tsx` — client island, sandboxed iframe (`sandbox="allow-same-origin"`, no `allow-scripts`)
- `webapp/apps/shell/components/gsg/ForbiddenPatternsAlert.tsx` — read-only V1 list with rationale
- `webapp/apps/shell/components/gsg/GsgRunPreview.tsx` — client island subscribing to Supabase Realtime `runs` filtered by `run_id`
- `webapp/apps/shell/components/gsg/HandoffBriefSection.tsx` — client island pairing wizard + GsgRunPreview
- `webapp/tests/e2e/gsg-design-grammar.spec.ts` — Playwright contract spec

**Files modifiés (3)**
- `webapp/apps/shell/app/gsg/page.tsx` — REWRITTEN as Design Grammar viewer Server Component
- `webapp/apps/shell/components/gsg/BriefWizard.tsx` — orphan `triggerGsgRun()` replaced with `<TriggerRunButton type="gsg" metadata={...} />` (now flows through admin-gated Task 002 backend)
- `webapp/apps/shell/lib/gsg-api.ts` — header marked DEPRECATED (kept for backward compat)

**Décisions notables (agent worktree)**
- Iframe sandbox : `sandbox="allow-same-origin"` only — tokens.css is CSS-only, no JS execution ; `referrerPolicy="no-referrer"` + bounded `max-height` defend against layout bombs
- Data source : disk via `loadDesignGrammar()` for V1 (worktree had no `data/captures/`, returns empty bundles gracefully) ; API route mirrors SP-11 dual-backend
- BriefWizard wire-up : swapped direct `triggerGsgRun()` fetch for `<TriggerRunButton type="gsg">` — passes `{client_slug, page_type, mode, audience}` metadata through Task 002 `POST /api/runs`
- GsgRunPreview Realtime : Postgres-side filter (`filter: 'id=eq.<runId>'`), same `subscribeRuns` helper as RunStatusPill (`/simplify` E1) ; iframe materialises only when `status === 'completed' && output_path != null`
- Server-only/pure split applied upfront (Sprint 6+7 webpack-boundary lesson) — zero post-merge bundle fix required
