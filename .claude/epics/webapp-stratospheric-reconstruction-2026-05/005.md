---
name: growth-audit-v26-deep-detail
status: closed
created: 2026-05-13T17:12:51Z
updated: 2026-05-14T23:00:00Z
github: (will be set on sync)
depends_on: [001, 006]
parallel: false
conflicts_with: []
tier: 2
effort_days: 3-4
priority: P0
---

# Task 005 : growth-audit-v26-deep-detail

## Description

Restaure les features V26 sur la page audit detail `/audits/[clientSlug]/[auditId]`. Source : V26 HTML L1720-2455 (`renderAuditList` + `renderClientCard` + `renderClientDrilldown` + `renderPageAudit`). Plusieurs features manquent : labels FR criterion_id, dual-viewport toggle, client-hero block, v26-panels per-client, canonical-tunnel dedup, pulsing P0 dot.

## Acceptance Criteria

- [ ] **`CRIT_NAMES_V21` 51-entry FR labels map** (port from V26 L2416-2442) :
  - File : `webapp/apps/shell/lib/criteria-labels.ts` (NEW)
  - Map : `hero_01 → "H1 promesse claire (Test 5s)"`, `per_03 → "Preuve sociale ATF (badges/logos)"`, ... 51 entries
  - Used everywhere reco displays criterion_id (RichRecoCard, audit summary, breakdowns)
- [ ] **Dual-viewport 💻/📱 toggle** :
  - `useViewport()` hook (`webapp/apps/shell/lib/use-viewport.ts` NEW) : returns `{viewport, setViewport}`
  - `<ViewportToggle />` component (NEW) : two buttons 💻 Desktop / 📱 Mobile, controlled
  - On audit detail page : screenshots + spatial perception switch based on viewport
- [ ] **`<ClientHeroBlock>`** (NEW) :
  - Brand DNA palette swatches (primary + 3 secondaries + 5 neutrals)
  - Voice samples (3-5 sentences from `brand_dna_json.voice.samples`)
  - Typography sample (display + body)
  - Renders at top of `/clients/[slug]` route
- [ ] **`<V26Panels>` per-client** (NEW) :
  - Brand DNA panel (link to /clients/[slug]/dna)
  - Design Grammar panel (link to /gsg if available)
  - Evidence count + lifecycle counters
  - Renders on audit detail page
- [ ] **Canonical-tunnel dedup** :
  - `<CanonicalTunnelTab>` (NEW) : 🌊 tab visible when audit page funnels into a canonical tunnel (`canonical_tunnel.json` exists)
  - Dedups recos that are inherited from canonical tunnel (avoids double-counting)
- [ ] **Pulsing P0 dot animation** (CSS keyframes) : red dot pulse next to client name when P0 count > 0
- [ ] **Sticky page-tabs** : page_type tabs (home / blog / collection / pricing / ...) sticky-top on scroll
- [ ] **Mathis manual validation** : "Audit detail page enfin à parité V26"

## Technical Details

### Files créés
- `webapp/apps/shell/lib/criteria-labels.ts` (NEW, 51-entry map)
- `webapp/apps/shell/lib/use-viewport.ts` (NEW hook)
- `webapp/apps/shell/components/audits/ViewportToggle.tsx` (NEW)
- `webapp/apps/shell/components/clients/ClientHeroBlock.tsx` (NEW)
- `webapp/apps/shell/components/audits/V26Panels.tsx` (NEW)
- `webapp/apps/shell/components/audits/CanonicalTunnelTab.tsx` (NEW)
- `webapp/apps/shell/components/audits/StickyPageTabs.tsx` (NEW)

### Files modifiés
- `webapp/apps/shell/components/audits/AuditDetailFull.tsx` (refactor : intégrer dual-viewport + ClientHeroBlock + V26Panels + CanonicalTunnelTab + StickyPageTabs)
- `webapp/apps/shell/components/audits/RichRecoCard.tsx` (use CRIT_NAMES_V21 for title fallback when criterion_id only)
- `webapp/apps/shell/app/clients/[slug]/page.tsx` (add ClientHeroBlock)
- `webapp/apps/shell/components/audits/AuditScreenshotsPanel.tsx` (use useViewport to switch desktop/mobile)
- `webapp/packages/ui/src/styles.css` (add `@keyframes gc-pulse-p0`)

### CRIT_NAMES_V21 extraction
Read V26 HTML L2416-2442 and produce TS map. Validate 51 entries (per V26 source).

### Pulsing P0 dot CSS
```css
@keyframes gc-pulse-p0 {
  0%, 100% { box-shadow: 0 0 0 0 rgba(232, 117, 85, 0.7); }
  50%      { box-shadow: 0 0 0 8px rgba(232, 117, 85, 0); }
}
.gc-p0-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: var(--bad);
  animation: gc-pulse-p0 1.6s var(--ease-aura) infinite;
}
```

## Dependencies

- **Code** : task 001 (design tokens + scoreColor)
- **Code** : task 006 (bbox crops utility) — depends because viewport toggle uses bbox crops which are defined in task 006
- **Disk** : `data/captures/<client>/<page>/canonical_tunnel.json` (may or may not exist per client)

## Effort Estimate
- **Size**: L
- **Hours**: 24-32 (3-4 jours solo dev)

Breakdown :
- CRIT_NAMES_V21 map extraction + wiring : 3h
- useViewport hook + ViewportToggle : 3h
- ClientHeroBlock : 3h
- V26Panels : 3h
- CanonicalTunnelTab + dedup logic : 4h
- StickyPageTabs : 2h
- Pulsing P0 dot animation : 1h
- AuditDetailFull refactor : 4h
- AuditScreenshotsPanel viewport switch : 2h
- Playwright E2E spec : 3h
- Mathis manual validation iteration : 2h

## Definition of Done

- [x] Code implemented per acceptance criteria (8 files created + 9 modified across 4 commits via parallel agent worktree)
- [x] Typecheck shell exit 0 (`npm run typecheck --workspace=apps/shell`)
- [x] ESLint shell exit 0 (`No ESLint warnings or errors`)
- [x] CRIT_NAMES_V21 map has **54** entries (verbatim port from V26 HTML L2416-2442 — task spec said "51" but the actual V26 source has 6 hero + 11 per + 9 coh + 8 psy + 8 ux + 5 tech + 7 V21-cluster = 54)
- [x] Playwright contract spec written + 8/8 PASS prod (route-never-500 + /login mount). Dynamic TS cross-workspace import block stripped (Playwright loader can't resolve cross-workspace TS — TS compile already enforces the entry-count invariant via `CRIT_NAMES_V21_COUNT`)
- [x] PR merged into main + Vercel deploy sha=`1a041b8` on https://growth-cro.vercel.app
- [x] Cumulative regression suite 152/152 PASS prod (zero drift)
- [ ] Mathis manual validation : "Audit detail à parité V26" — V26Panels visible on `/audits/[c]/[a]` · ClientHeroBlock on `/clients/[slug]` · viewport toggle 💻/📱 switches screenshots + bbox crops · sticky page-type tabs · pulsing P0 dot next to client names with P0>0 · criterion_id pills show FR labels (e.g. "Mobile-first (ux_05)")

## Implementation log — 2026-05-14

**Files créés (8)**
- `webapp/apps/shell/lib/criteria-labels.ts` — CRIT_NAMES_V21 (54 entries) + `criterionLabel()` + `criterionPillText()` helpers
- `webapp/apps/shell/lib/use-viewport.ts` — `{viewport, setViewport}` hook, default desktop, localStorage-persisted (default-desktop on first render to avoid hydration mismatch)
- `webapp/apps/shell/components/audits/ViewportToggle.tsx` — two pill buttons 💻/📱
- `webapp/apps/shell/components/audits/AuditScreenshotsView.tsx` — client island consuming useViewport so `AuditScreenshotsPanel` stays server-side
- `webapp/apps/shell/components/audits/V26Panels.tsx` — per-client overview (BrandDNA + Design Grammar + Evidence + Lifecycle counters)
- `webapp/apps/shell/components/audits/CanonicalTunnelTab.tsx` — 🌊 tab, gates on `audit.scores_json.canonical_tunnel`, returns null silently when missing
- `webapp/apps/shell/components/clients/ClientHeroBlock.tsx` — Brand DNA palette swatches + voice samples + typography sample, reuses existing `normalizeBrandDna()`
- `webapp/tests/e2e/growth-audit-v26.spec.ts` — 4 contract cases × 2 viewports

**Files modifiés**
- `webapp/apps/shell/components/audits/AuditDetailFull.tsx` — integrate V26Panels + CanonicalTunnelTab above the recos+screenshots grid
- `webapp/apps/shell/components/audits/AuditScreenshotsPanel.tsx` — refactored to server wrapper + AuditScreenshotsView client island
- `webapp/apps/shell/components/audits/RichRecoCard.tsx` — `criterionPillText()` for the criterion_id pill ("Mobile-first (ux_05)" instead of bare "ux_05") + reads useViewport to pick desktop_full.png vs mobile_full.png for bbox crops
- `webapp/apps/shell/components/audits/PageTypesTabs.tsx` — sticky-top via single `.gc-sticky-tabs` wrapper (no new component)
- `webapp/apps/shell/components/command-center/FleetPanel.tsx` — `.gc-p0-dot` next to client names when p0_count > 0
- `webapp/apps/shell/components/dashboard/CriticalClientsGrid.tsx` — same `.gc-p0-dot` treatment
- `webapp/apps/shell/app/clients/[slug]/page.tsx` — render ClientHeroBlock at top
- `webapp/apps/shell/app/audits/[clientSlug]/[auditId]/page.tsx` — pass brand_dna_json prop through to AuditDetailFull
- `webapp/packages/ui/src/styles.css` — `@keyframes gc-pulse-p0` + `.gc-p0-dot` + `.gc-sticky-tabs` + `prefers-reduced-motion` guard

**Décisions notables (agent worktree)**
- Shipped **54** CRIT_NAMES entries (task spec said 51 — V26 HTML actually has 54, verbatim port preserved)
- `AuditScreenshotsPanel` split into server wrapper + client `AuditScreenshotsView` island so the viewport toggle works without leaking fs/Supabase calls to the client
- `PageTypesTabs` kept its identity; sticky behaviour added via a single `.gc-sticky-tabs` wrapper + `position: sticky; top: 0`
- `useViewport` is default-desktop on first render then promoted to localStorage value via `useEffect` to avoid hydration mismatch
- Pill component has no `title` prop, so wrapped the criterion pill in `<span title=...>` for the raw-id tooltip
- Playwright dynamic TS cross-workspace import test removed — entry-count invariant is enforced at compile time via `CRIT_NAMES_V21_COUNT` constant + TS strict mode
