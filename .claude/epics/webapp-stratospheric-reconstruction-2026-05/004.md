---
name: dashboard-v26-closed-loop-narrative
status: open
created: 2026-05-13T17:12:51Z
updated: 2026-05-13T17:12:51Z
github: (will be set on sync)
depends_on: [001]
parallel: true
conflicts_with: []
tier: 2
effort_days: 3-4
priority: P0
---

# Task 004 : dashboard-v26-closed-loop-narrative

## Description

Restaure le dashboard V26 complet sur la home `/` route. Source : V26 HTML L900-959 (`<section class="pane active" id="pane-dashboard">`). Le V26 dashboard a un narrative spécifique : KPI grid → Closed-Loop coverage strip → 3 tabs (fleet / business / pagetype) avec charts + tables.

Actuellement la home Next.js a `<CommandCenterTopbar>` + `<CommandCenterKpis>` + `<FleetPanel>` + `<ClientHeroDetail>` — proche mais incomplet (manque Closed-Loop strip, manque pillar bars chart, manque tabs business/pagetype).

## Acceptance Criteria

- [ ] **Closed-Loop coverage strip 8 KPIs** (`<ClosedLoopStrip />` NEW) per V26 L909-919 :
  - 8 cards horizontale : Evidence · Lifecycle · BrandDNA · DG · Funnel · Reality · GEO · Learning
  - Per card : module name + status badge (✓ active / ⚠ partial / ❌ pending) + counter (e.g. "51/56 brand_dna")
  - Sub-title "État réel des 7 piliers V26 + V29/V30 sur la fleet"
- [ ] **Tabs dashboard** : Fleet / Business / Page-type (`<DashboardTabs />` NEW)
- [ ] **Tab Fleet** (default) :
  - 6 pillars fleet bar chart `<PillarBarsFleet />` (NEW) — moyenne pondérée per pillar (hero/persuasion/ux/coherence/psycho/tech + utility_banner)
  - Priority distribution chart `<PriorityDistribution />` (NEW) — P0/P1/P2/P3 horizontal bars
  - Critical clients grid (top 10 par P0 count)
- [ ] **Tab Business** :
  - Business type breakdown table `<BusinessBreakdownTable />` (NEW) : clients, pages, P0 blockers, score moyen per business_category
- [ ] **Tab Page-type** :
  - Page type breakdown table `<PageTypeBreakdownTable />` (NEW) : clients, n_pages, n_recos, avg_score per page_type
- [ ] **KPI grid header** mis à jour : Cormorant Garamond italic + gold gradient values (depends task 001 design DNA)
- [ ] **HSL score colors** appliqués partout via `scoreColor()` utility de task 001
- [ ] **Realtime updates** : KPI counters et tables se rafraîchissent quand un run completes (depends task 002 Realtime channel)
- [ ] **Lighthouse Performance ≥85** on `/` (charts ne doivent pas casser le budget)

## Technical Details

### Files créés
- `webapp/apps/shell/components/dashboard/ClosedLoopStrip.tsx` (NEW)
- `webapp/apps/shell/components/dashboard/DashboardTabs.tsx` (NEW)
- `webapp/apps/shell/components/dashboard/PillarBarsFleet.tsx` (NEW)
- `webapp/apps/shell/components/dashboard/PriorityDistribution.tsx` (NEW)
- `webapp/apps/shell/components/dashboard/BusinessBreakdownTable.tsx` (NEW)
- `webapp/apps/shell/components/dashboard/PageTypeBreakdownTable.tsx` (NEW)
- `webapp/apps/shell/components/dashboard/queries.ts` (extend existing)

### Files modifiés
- `webapp/apps/shell/app/page.tsx` (orchestrate new components)
- `webapp/apps/shell/components/command-center/CommandCenterKpis.tsx` (apply Cormorant italic gradient)
- `webapp/packages/data/src/queries/aggregates.ts` (NEW or extend) : SQL queries for closed-loop coverage + business breakdown + page-type breakdown

### Closed-Loop coverage data sources
- Evidence : count `evidence_ledger.json` files on disk OR `evidence_count` column si migrated
- Lifecycle : count recos with `lifecycle_status != 'backlog'`
- BrandDNA : count clients with `brand_dna_json IS NOT NULL`
- DG (Design Grammar) : count clients with `design_grammar` artefacts (from disk for now)
- Funnel : count audits with `funnel_json IS NOT NULL`
- Reality : count clients with `reality_credentials` set (or always 0 for now until task 011)
- GEO : count clients with `geo_audits` rows (task 009 dependency)
- Learning : count `doctrine_proposals` with `review IS NOT NULL`

### Chart implementation
- Use existing approach (Recharts not in deps; current charts in `/funnel/` use pure SVG inline)
- Continue pure SVG inline pattern (zero new dep, no bundle bloat)
- KPI value gradient applied via class `gc-kpi-value` (defined in task 001)

## Dependencies

- **Code** : task 001 must be merged (uses new tokens + gradient utility + scoreColor)
- **Code** : task 002 Realtime channel for live updates (optional, can fallback to refresh-on-mount)
- **External** : none

## Effort Estimate
- **Size**: M-L
- **Hours**: 24-32 (3-4 jours solo dev)

Breakdown :
- Closed-Loop coverage strip 8 KPIs : 4h
- Tabs component : 2h
- 6 pillars fleet chart (SVG) : 4h
- Priority distribution chart (SVG) : 3h
- Business breakdown table + SQL queries : 4h
- Page-type breakdown table + SQL queries : 4h
- Critical clients grid : 2h
- Realtime subscriptions wiring : 2h
- Playwright E2E + visual snapshot : 3h
- Mathis manual validation : 2h

## Definition of Done

- [ ] Code implemented per acceptance criteria
- [ ] Typecheck shell exit 0
- [ ] Lighthouse Performance ≥85 on `/`
- [ ] Playwright E2E spec : tabs switch + Closed-Loop strip 8 KPIs visible + KPI values gradient
- [ ] Mathis manual validation : "Dashboard ressemble enfin à V26"
- [ ] PR merged + Vercel deploy
