---
name: design-dna-v22-stratospheric-recovery
status: closed
created: 2026-05-13T17:12:51Z
updated: 2026-05-14T13:37:58Z
closed: 2026-05-13T18:00:00Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
tier: 1
effort_days: 3-4
priority: P0
commits: [358a75e, 772961e]
playwright_validation: visual-dna-v22.spec.ts 7/7 PASS prod
---

# Task 001 : design-dna-v22-stratospheric-recovery

## Description

Restaurer le visual DNA V22 "Stratospheric Observatory" perdu lors du port V26 HTML → Next.js. Source canonique : `deliverables/GrowthCRO-V26-WebApp.html` L17-100 (CSS custom properties + typography + spacing + animations). Cette task touche tous les composants existants via `@growthcro/ui` mais introduit pas de nouveau feature — c'est une refonte du design system.

Cette task est BLOCKING pour TIER 2 (toutes les tasks dépendent du nouveau design system).

## Acceptance Criteria

- [ ] **4 typefaces chargées** via `next/font/google` dans `app/layout.tsx` :
  - Cormorant Garamond (display serif, weights 300-600 + italics)
  - Playfair Display (display, weights 400-600 + italics)
  - Inter (body, weights 300-700)
  - JetBrains Mono (metrics/code, weights 400-500)
- [ ] **Palette Alaska Boreal Night** dans `packages/ui/src/styles.css` :
  - `--night-abyss #000207` · `--night-deep #020510` · `--night-mid #050c22` · `--night-elev #0b1634` · `--night-glow #122a5a`
  - `--aurora #4d8fff` · `--aurora-violet #8c7ef1` · `--aurora-cyan #6ee0df`
  - `--gold-sunset #e8c872` (signature) · `--gold #d4a945` · `--gold-deep #b8863a` · `--bronze #a67b3e`
  - `--star #fbfaf5` · `--star-warm #f6eed6` · `--star-dim rgba(251,250,245,0.62)` · `--star-faint rgba(251,250,245,0.18)`
- [ ] **Spacing golden ratio φ≈1.618** : `--sp-0 0.236rem` à `--sp-7 6.854rem`
- [ ] **KPI value gradient** utility class : `linear-gradient(135deg, #fbfaf5 0%, #e8c872 55%, #d4a945 100%)` + `drop-shadow(0 0 18px rgba(232,200,114,0.15))`
- [ ] **Glass cards** : `backdrop-filter: blur(22px) saturate(160%)` + `border: 1px solid rgba(232,200,114,0.12)` (`--glass-border-strong rgba(232,200,114,0.28)` for emphasis)
- [ ] **Background 4 couches** dans `body::before` + `body::after` per V26 L104-119 (radial-gradient pollution lumineuse horizon + hint bleu nuit + linear-gradient base noir profond + vignette)
- [ ] **Starfield canvas component** `<StarfieldBackground />` (4 layers parallax + shooting stars + 1.8s fade-in on load) — port from V26 `<canvas id="starfield-canvas">` script
- [ ] **Score color utility** `scoreColor(pct)` dans `packages/ui/src/score-color.ts` : `hsl(pct/100*120, 65%, 55%)` continuous gradient red→green
- [ ] **Aura easing** Tailwind/CSS variables : `--ease-aura cubic-bezier(0.23, 1, 0.32, 1)` · `--ease-snap` · `--ease-inertia`
- [ ] **Migration legacy `--gc-*` palette** : grep + replace toutes occurrences vers nouveaux tokens. Garder `--gc-*` comme aliases temporaires (1 sprint) pour pas casser le build, puis cleanup en task 015.
- [ ] **Lighthouse Performance ≥85** sur `/` (perf budget pour `backdrop-filter` + canvas)
- [ ] **Mathis manual validation** : "OUI ça ressemble enfin à la V26"

## Technical Details

### Files créés
- `webapp/packages/ui/src/components/StarfieldBackground.tsx` (NEW)
- `webapp/packages/ui/src/score-color.ts` (NEW)
- `webapp/packages/ui/src/lib/typography.ts` (NEW, fonts export)
- `webapp/packages/ui/src/lib/spacing.ts` (NEW, φ-spacing utility)

### Files modifiés
- `webapp/packages/ui/src/styles.css` : refonte tokens + 4-layer bg + glass + KPI gradient classes
- `webapp/apps/shell/app/layout.tsx` : add `<StarfieldBackground />` + 4 next/font imports + className inheritance
- `webapp/packages/ui/src/index.ts` : export new components
- `webapp/apps/shell/app/globals.css` : remove redundant tokens, defer to ui/styles.css

### Files touched massively (legacy `--gc-*` → new tokens)
- Tous les `.tsx` sous `webapp/apps/shell/components/`
- Tous les `.tsx` sous `webapp/apps/shell/app/`
- Estimated ~50-70 components

### Strategy de migration legacy → V22
1. Phase 1 : add new tokens **alongside** old (no break)
2. Phase 2 : add CSS alias rules (`--gc-bg = var(--night-abyss)`) for backward compat
3. Phase 3 : grep + replace `--gc-*` → new tokens via sed (verify with playwright snapshot test)
4. Phase 4 : remove aliases in task 015 cleanup

### Starfield component design
```typescript
// packages/ui/src/components/StarfieldBackground.tsx
"use client";
import { useEffect, useRef } from "react";

export function StarfieldBackground() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  useEffect(() => {
    // 4 layers parallax stars + shooting stars + 1.8s fade-in
    // port from V26 HTML L854-856 + <script> starfield init
  }, []);
  return <canvas ref={canvasRef} id="starfield-canvas" aria-hidden="true" />;
}
```

## Dependencies

- **External (Mathis)** : 1h Mathis validation visuelle finale
- **Code** : pas de dépendance code, première task TIER 1

## Effort Estimate
- **Size**: L
- **Hours**: 24-32 (3-4 jours solo dev)

Breakdown :
- Tokens + palette + spacing : 4h
- Typography (4 fonts + responsive scale) : 3h
- Glass cards + KPI gradient + score color utility : 4h
- 4-layer background : 2h
- Starfield canvas port : 6h
- Migration legacy → new tokens (50-70 components) : 8-12h
- Playwright visual snapshot tests : 3h
- Lighthouse perf budget validation + tuning : 2h
- Mathis manual validation iteration : 2h

## Definition of Done

- [ ] Code implemented per acceptance criteria
- [ ] Typecheck shell : `npx tsc --noEmit` exit 0
- [ ] Lint hygiene : `python3 scripts/lint_code_hygiene.py --staged` exit 0
- [ ] Playwright spec : `webapp/tests/e2e/visual-dna-v22.spec.ts` (NEW) — verify font-family computed on H1/H2 = Cormorant + gradient on KPI values + starfield canvas visible
- [ ] Lighthouse Performance ≥85 on `/` (via `npx lhci autorun` or manual)
- [ ] Visual diff before/after screenshots committed to PR description
- [ ] Mathis manual validation comment on PR : "OUI ça ressemble enfin à V26"
- [ ] PR merged to main
- [ ] Vercel auto-deploy completed + smoke-tested on prod
