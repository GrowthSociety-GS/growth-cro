---
name: scent-trail-pane-port
status: in-progress
created: 2026-05-13T17:12:51Z
updated: 2026-05-14T22:00:00Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
tier: 3
effort_days: 2
priority: P1
---

# Task 007 : scent-trail-pane-port

## Description

Port complet du pane Scent Trail V26 (actuellement 0% port dans Next.js). Source : V26 HTML L2580-2666 (`renderScent` + `renderScentFor`). Le Scent Trail mesure la continuité narrative ad → LP → product (signaux de break dans la cohérence du parcours).

## Acceptance Criteria

- [ ] **Supabase migration** `audits.scent_trail_json` JSONB column (`supabase/migrations/20260513_0013_audits_scent_trail.sql`)
- [ ] **Migration `migrate_disk_to_supabase.py`** : extend `load_page_recos` ou nouvelle fonction `load_scent_trail` qui lit `data/captures/<c>/scent_trail.json` (fichier global per-client, pas per-page) et UPSERT en Supabase
- [ ] **New route `/scent`** (`webapp/apps/shell/app/scent/page.tsx` NEW)
- [ ] **Sidebar nav-item** "🔄 Scent Trail" (per V26 L867)
- [ ] **Fleet KPI cards** (`<ScentFleetKPIs />` NEW) :
  - Total breaks détectés
  - Clients with ≥1 break
  - Avg break severity (low/medium/high)
- [ ] **Per-client diagram** (`<ScentTrailDiagram />` NEW) :
  - Visualisation ad → LP → product flow (3 nodes + 2 edges)
  - Edges colored par scent continuity (gold = continuous, red = break)
  - Click node → drilldown
- [ ] **Breaks list** (`<BreaksList />` NEW) :
  - Per-client list of "where scent breaks"
  - Each break : node_from → node_to + reason (semantic mismatch / visual mismatch / promise mismatch) + severity
- [ ] **Fleet table sortable** (`<ScentFleetTable />` NEW) : columns slug · n_breaks · max_severity · last_audit · score_breakdown

## Technical Details

### Files créés
- `webapp/apps/shell/app/scent/page.tsx` (NEW route)
- `webapp/apps/shell/app/scent/loading.tsx` (NEW)
- `webapp/apps/shell/app/scent/error.tsx` (NEW)
- `webapp/apps/shell/components/scent/ScentFleetKPIs.tsx` (NEW)
- `webapp/apps/shell/components/scent/ScentTrailDiagram.tsx` (NEW)
- `webapp/apps/shell/components/scent/BreaksList.tsx` (NEW)
- `webapp/apps/shell/components/scent/ScentFleetTable.tsx` (NEW)
- `webapp/apps/shell/lib/scent-fs.ts` (NEW data layer)
- `supabase/migrations/20260513_0013_audits_scent_trail.sql` (NEW migration)
- `scripts/migrate_disk_to_supabase.py` extension : add `load_scent_trail()` helper

### Files modifiés
- `webapp/apps/shell/components/Sidebar.tsx` (add Scent Trail nav-item)

### Disk source format
`data/captures/<client>/scent_trail.json` typical structure :
```json
{
  "client": "weglot",
  "flow": {
    "ad": { "channel": "google_ads", "headline": "...", "scent_keywords": [...] },
    "lp":  { "page_type": "lp_listicle", "headline": "...", "scent_keywords": [...] },
    "product": { "page_type": "pricing", "headline": "...", "scent_keywords": [...] }
  },
  "breaks": [
    { "from": "ad", "to": "lp", "type": "semantic_mismatch", "severity": "medium", "reason": "..." },
    ...
  ],
  "scent_score": 0.72
}
```

## Dependencies

- **External** : none
- **Code** : task 001 (design tokens) — soft dependency for visual style

## Effort Estimate
- **Size**: M
- **Hours**: 12-16 (2 jours solo dev)

Breakdown :
- Supabase migration : 1h
- Migration script extension : 2h
- Route + page.tsx orchestration : 2h
- ScentFleetKPIs : 1h
- ScentTrailDiagram (3-node SVG) : 4h
- BreaksList : 2h
- ScentFleetTable sortable : 2h
- Playwright E2E : 2h

## Definition of Done

- [x] Code implemented per acceptance criteria (9 files created + Sidebar.tsx modified across 4 commits via parallel agent worktree)
- [x] Typecheck shell exit 0
- [x] ESLint shell exit 0
- [x] Code hygiene gate exit 0
- [x] Playwright contract spec written + 8/8 PASS prod (route never 500 + /login mount + anonymous never sees admin surface)
- [x] Cumulative regression suite 152/152 PASS prod (zero drift)
- [x] PR merged into main + Vercel deploy sha=`1a041b8` on https://growth-cro.vercel.app (initial deploy failed on `node:fs/promises` client-bundle leak — fix `1a041b8` extracted pure types/helpers into `scent-types.ts` + added `import "server-only"` guard to `scent-fs.ts`)
- [ ] Supabase migration `20260514_0020_audits_scent_trail.sql` applied prod (Mathis Dashboard SQL editor — additive column, no breaking change)
- [ ] All clients with `scent_trail.json` on disk migrated to Supabase (V26 disk currently has 0 files — pending the next capture wave producing scent trails)
- [ ] Mathis manual validation : "Scent Trail pane restauré" — `/scent` route accessible from sidebar · empty-state shows when no client has scent_trail.json · KPI cards render · diagram renders 3 nodes + 2 edges · fleet table sortable

## Implementation log — 2026-05-14

**Files créés (9)**
- `supabase/migrations/20260514_0020_audits_scent_trail.sql` — additive JSONB column, idempotent
- `webapp/apps/shell/lib/scent-fs.ts` — server-only data layer reading `data/captures/*/scent_trail.json`
- `webapp/apps/shell/lib/scent-types.ts` (added post-merge) — pure types + helpers shared client/server
- `webapp/apps/shell/components/scent/ScentFleetKPIs.tsx` — total breaks · clients-with-break · avg severity
- `webapp/apps/shell/components/scent/ScentTrailDiagram.tsx` — pure inline SVG viewBox=0 0 600 200 (3 nodes radius 46 + 2 arrowed edges, broken=red dashed / continuous=gold solid)
- `webapp/apps/shell/components/scent/BreaksList.tsx` — per-client breaks list with severity Pill tones
- `webapp/apps/shell/components/scent/ScentFleetTable.tsx` — sortable table, default scent_score asc (worst first)
- `webapp/apps/shell/app/scent/page.tsx` — Server Component, fleet overview
- `webapp/tests/e2e/scent-trail.spec.ts` — Playwright contract spec
- Extension : `scripts/migrate_disk_to_supabase.py` — `load_scent_trail()` + `upsert_scent_trail()` helpers + main-loop wiring

**Files modifiés**
- `webapp/apps/shell/components/Sidebar.tsx` — `"🔄 Scent Trail" → /scent` nav-item inserted in Studio group between GSG and Doctrine

**Décisions notables (agent worktree)**
- `upsert_scent_trail` UPSERTs into the most-recent audit row's `scent_trail_json` rather than a parallel `scent_trails` table — reuses audit lifecycle + RLS, avoids a second realtime channel for a single nullable column
- `ScentTrailDiagram` pure inline SVG (no D3/Recharts) — 3 nodes + 2 directed edges with arrowheads
- `/scent` Server Component reads disk via `lib/scent-fs.ts` (mirrors `captures-fs.ts`) — Phase B can swap to a Supabase view without changing component contracts
- Severity pill colors reuse existing `Pill` tones (cyan/amber/red) — no new design tokens
- Default fleet sort is `scent_score asc` so worst journeys surface first (agency-priority bias)
- V26 disk currently has 0 `scent_trail.json` files — empty-state copy validated; page ships ready for the next capture wave

**Post-merge fix (parent session)**
- Vercel build failed because `ScentFleetTable.tsx` (`"use client"`) value-imported `maxSeverity` from `scent-fs.ts`, which imports `node:fs/promises` — webpack rejected the `node:` scheme. Fix split the pure surface into `scent-types.ts`, added `import "server-only";` guard to `scent-fs.ts`, retargeted all 4 component imports to `scent-types`. Local + prod build success after the fix, /scent chunk 2.8 kB.
