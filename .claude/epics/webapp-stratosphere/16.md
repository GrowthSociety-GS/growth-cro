---
name: Webapp Architecture Map (YAML + Mermaid, vivant)
status: closed
created: 2026-05-11T09:21:13Z
updated: 2026-05-11T09:30:01Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/16
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Webapp Architecture Map (YAML + Mermaid, vivant)

## Description

Livrer une map machine-readable + visuelle de TOUTE l'architecture GrowthCRO post-cleanup-epic. Chaque module avec son purpose, inputs typés, outputs typés, depends_on, imported_by, doctrine_refs, status, lifecycle_phase. Auto-générée à chaque epic terminé. Source-of-truth unique pour toute conversation Claude/Codex future.

Référence PRD : FR-1 (US-1).

## Acceptance Criteria

- [ ] `.claude/docs/state/WEBAPP_ARCHITECTURE_MAP.yaml` existe avec :
  - Section `modules:` avec toutes les unités `growthcro/`, `moteur_gsg/`, `moteur_multi_judge/`, `skills/`, `scripts/`, `deliverables/`, `playbook/`, `data/`, `SCHEMA/`
  - Pour chaque module : `path, purpose, inputs, outputs, depends_on, imported_by, doctrine_refs, status (active|legacy|deprecated), lifecycle_phase (onboarding|runtime|qa|learning|infrastructure)`
  - Section `data_artefacts:` listant `data/captures/<client>/<page>/*.json` types et leur producer module
  - Section `pipelines:` décrivant les flux end-to-end (audit pipeline, GSG pipeline, multi-judge, reality loop)
- [ ] `.claude/docs/state/WEBAPP_ARCHITECTURE_MAP.md` existe avec :
  - Diagramme Mermaid global (vue programme entier)
  - 5 diagrammes détaillés : audit pipeline / GSG pipeline / multi-judge / webapp / reality+experiment+learning
- [ ] `scripts/update_architecture_map.py` créé. Scanne AST de `growthcro/` + `moteur_gsg/` + `moteur_multi_judge/` + `skills/`. Regénère le YAML. Exit 0.
- [ ] `CLAUDE.md` init step §10 référence `WEBAPP_ARCHITECTURE_MAP.yaml` comme lecture obligatoire en début de session.
- [ ] Linter, parity, schemas, capabilities-keeper tous green.
- [ ] MANIFEST §12 changelog entry.

## Technical Details

- AST scan via `ast` stdlib pour extraire imports + classes/functions top-level
- Output YAML hand-curated pour première version (le script v1 fait le squelette, Mathis affine purpose+inputs+outputs en review)
- v2 du script : update incrémental (préserve les `purpose` curated tout en refresh des `imports/exports`)
- Mermaid : utiliser flowchart TD (top-down) avec subgraphs par couche
- Pas de nouvelle feature de produit — juste documentation infrastructure

## Dependencies

- Tout existing post cleanup-epic + alignment Codex V27.2-G
- Aucune dépendance sur skills externes

## Effort Estimate

- Size: S
- Hours: 16-24 (2-3 jours)

## Definition of Done

- [ ] YAML + MD livrés avec contenu réel (pas placeholder)
- [ ] `update_architecture_map.py` testé, exit 0
- [ ] CLAUDE.md mis à jour (init step §10)
- [ ] Gates standards verts (lint + parity + schemas + capabilities)
- [ ] MANIFEST §12 entry
- [ ] Mathis approuve : "la photo est claire, je m'y retrouve"
