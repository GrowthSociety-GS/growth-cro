# doctrine-keeper verdict — Issue #19 GSG Stratosphere

**Date:** 2026-05-11
**Status:** ✅ APPROVED (no doctrine drift)
**Reviewer pattern:** `.claude/agents/doctrine-keeper.md` (cross-check executed manually — direct sub-agent invocation not available in this session).

## What was changed

| Surface | Change | Doctrine impact |
|---|---|---|
| `moteur_gsg/core/animations.py` | NEW (291 LOC) | None — pure CSS motion layer, no playbook reference, no scorer hook. |
| `moteur_gsg/core/impeccable_qa.py` | NEW (395 LOC) | None — anti-pattern catalogue derives from external `Impeccable` skill (combo "GSG generation"), not from `playbook/bloc_*_v3.json`. Adds a NEW post-render gate, does not modify the doctrine_judge weighting. |
| `moteur_gsg/core/page_renderer_orchestrator.py` | Wires `animations.render_animations_css` | None — append-only CSS, no logic change. |
| `moteur_gsg/core/section_renderer.py` | Wires `animations.render_animations_css` | None — append-only CSS. |
| `moteur_gsg/modes/mode_1_complete.py` | Wires `impeccable_qa.run_impeccable_qa` post-`apply_minimal_postprocess`, pre-`run_multi_judge`. Asset-ref worktree robustness. | None — additive QA gate, no scorer or recos touchpoint. |
| `scripts/test_gsg_regression.sh` | NEW (187 LOC) | None — orchestrates the existing `moteur_multi_judge` against the existing Weglot baseline. No new doctrine artefact, no playbook write. |
| `deliverables/gsg_stratosphere/*.html` | 3 scaffolded LPs | None — generated by the same V27.2-G+ pipeline that already consumes V3.2.1 doctrine via `doctrine_planner` (backward-compat AD-4 preserved). |
| `deliverables/gsg_stratosphere/screenshots/*` | Playwright proof | None. |
| `WEBAPP_ARCHITECTURE_MAP.{yaml,md}` | Registers new modules + pipeline stages + data artefacts | None — meta only. |

## Cross-check against doctrine-keeper hard rules

1. **"No file >800 LOC in active paths."**
   * `animations.py` = 291 LOC ✓
   * `impeccable_qa.py` = 395 LOC ✓ (≤ 400 target per task #19 spec)
   * `test_gsg_regression.sh` = 187 LOC ✓ (≤ 200 cap)
   * No existing file pushed over 800 LOC.

2. **"No `os.environ` / `os.getenv` outside `growthcro/config.py`."**
   * `animations.py`: no env access ✓
   * `impeccable_qa.py`: no env access ✓
   * `test_gsg_regression.sh`: bash script, not Python — rule doesn't apply ✓

3. **"No `_archive*` / `_obsolete*` / `*deprecated*` / `*backup*` folder inside an active path."**
   * No archive folders introduced ✓

4. **"No basename duplicates in active paths."**
   * `animations.py` — unique ✓
   * `impeccable_qa.py` — unique ✓
   * `test_gsg_regression.sh` — unique ✓

## Cross-check doctrine ↔ code (V3.2.1 + V3.3 backward-compat)

* **Pillar scorers** (`growthcro/scoring/pillars.py`, `skills/site-capture/scripts/score_*.py`):
  Not touched. V3.2.1 scoring unchanged.
* **Specific criteria** (`growthcro/scoring/specific/*.py`):
  Not touched.
* **UX pillar** (`growthcro/scoring/ux.py`):
  Not touched.
* **Page-type orchestrator** (`skills/site-capture/scripts/score_page_type.py`):
  Not touched.
* **Reco enricher** (`growthcro/recos/*.py`):
  Not touched.
* **Reco doctrine cache** (`growthcro/recos/schema.py`):
  Not touched. Still supports `doctrine_version='3.2.1'` and `doctrine_version='3.3'` (Issue #18 landing).
* **Dashboard build** (`skills/site-capture/scripts/build_growth_audit_data.py`):
  Not touched.
* **Manifest** (`docs/reference/GROWTHCRO_MANIFEST.md`):
  §12 entry to be added in a SEPARATE commit per CLAUDE.md.

## V26.AF doctrine — defensively verified

`moteur_gsg/modes/mode_1/prompt_assembly.py::MAX_SYSTEM_TOTAL_CHARS = 8192`
imports cleanly:

    python3 -c "from moteur_gsg.modes.mode_1.prompt_assembly import build_persona_narrator_prompt, MAX_SYSTEM_TOTAL_CHARS; print(MAX_SYSTEM_TOTAL_CHARS)"
    # → 8192

Neither `animations.py` nor `impeccable_qa.py` touches the persona prompt
assembly. The Impeccable layer is a deterministic offline QA gate (no
`system_messages` contribution, no LLM call). The Emil Kowalski motion
layer is a CSS string appended to the renderer stylesheet (no prompt
contribution).

## V3.3 consumption

V3.3 backward-compat (epic.md AD-4): the 56 existing audited clients
remain scored V3.2.1 until their next audit. New audits opt into V3.3
via `recos.schema.load_doctrine(doctrine_version='3.3')`. Issue #19
**does not touch** this dispatch — it only adds layers BELOW the
doctrine consumption path (post-plan, post-render).

The 3 scaffolded stratosphere LPs (japhy-pdp, stripe-pricing,
linear-leadgen) used `copy_fallback_only=True` so no doctrine LLM
call happened. Their `doctrine_pack` was built deterministically by
`doctrine_planner.build_doctrine_pack` against the default V3.2.1
playbook (consistent with the rest of the 56 fleet).

## Verdict

**APPROVED.** Issue #19 lands two additive layers + 3 scaffolded
deliverables + 1 regression gate. No doctrine drift, no scorer
regression, no V26.AF leak, no V3.3 unsafe override.

Recommendation: live-run can proceed when `ANTHROPIC_API_KEY` is
available. Re-run `bash scripts/test_gsg_regression.sh` after live
generation to confirm the multi-judge regression budget (≤ 5pt vs
Weglot 70.9% baseline) is respected.

## Skills invoked

Per `SKILLS_INTEGRATION_BLUEPRINT.md` combo "GSG generation":
* `frontend-design` — implicit during `animations.py` design.
* `brand-guidelines` — implicit (per-client; deterministic via brand_dna).
* `Emil Kowalski Design Skill` — materialised as CSS in `animations.py`.
* `Impeccable` — materialised as detector logic in `impeccable_qa.py`.

Max 4 skills active. No cacophonic combinations (no Taste Skill,
no theme-factory, no lp-creator parallel). Anti-cacophonie rules
respected (CLAUDE.md anti-pattern #12).
