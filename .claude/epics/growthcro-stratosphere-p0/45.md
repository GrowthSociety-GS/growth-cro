---
name: Create SKILLS_REGISTRY_GOVERNANCE.json + audit script
status: closed
created: 2026-05-16T12:14:34Z
updated: 2026-05-16T12:43:15Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/45
depends_on: [42, 43, 44]
parallel: false
conflicts_with: []
---

# Task: Create `SKILLS_REGISTRY_GOVERNANCE.json` + audit script

## Description

Créer un **registry de gouvernance** distinct de `CAPABILITIES_REGISTRY.json` (qui reste code-only, fichiers Python actifs). Ce nouveau registry est **doctrine-gouvernance** : il documente chaque skill / agent / heuristique avec son **type honnête**, son **invocation_proof** (grep result), et un **security audit**.

L'audit Explore parallèle a démasqué que les 3 skills "actifs" (impeccable, cro-methodology, emil-design-eng) sont en réalité du **Python local** (`run_*()` functions), pas des skills Anthropic invoqués via Skill tool. 14 skills externes sont dormants. Il faut **assumer** ou **convertir** — ce registry sert à arrêter de prétendre.

Source : `AUDIT_DECISION_DOSSIER_2026-05-16` §5.1-5.3 + §10.3 + P0.4.

## Acceptance Criteria

- [ ] Fichier `growthcro/SKILLS_REGISTRY_GOVERNANCE.json` créé (toplevel repo, pas dans `.claude/`)
- [ ] Schema JSON respecté pour chaque entrée :
  ```json
  {
    "id": "<skill-or-agent-id>",
    "type": "installed_external_active | installed_external_dormant | installed_custom | runtime_heuristic_python | subagent | mcp_server | archived",
    "location": "<path>",
    "invocation_proof": "<file:line OR 'no occurrence found via grep'>",
    "llm_cost": "<0 | low | medium | high>",
    "security": {
      "shell": false,
      "network": false,
      "env_vars": false,
      "secrets": false,
      "git": false,
      "filesystem": "read_only | read_write | none"
    },
    "audit_date": "2026-05-16",
    "audit_method": "static_grep | dynamic_test | doc_only",
    "notes": "<free text>"
  }
  ```
- [ ] **17 entrées skills externes** (depuis `skills-lock.json`) auditées :
  - 3 actifs en Python : impeccable, cro-methodology, emil-design-eng → type=`runtime_heuristic_python`, invocation_proof grep result
  - 14 dormants → type=`installed_external_dormant`, invocation_proof="no occurrence found via grep"
- [ ] **10 entrées skills projet** (sous `skills/` racine) auditées :
  - 9 actifs (site-capture, gsg, cro-auditor, client-context-manager, webapp-publisher, audit-bridge-to-gsg, cro-library, growthcro-strategist, skill-based-architecture) → type=`installed_custom`
  - 1 frozen (mode-1-launcher) → type=`archived`
  - 1 lab legacy (growth-site-generator) → type=`runtime_heuristic_python` ou `archived`
- [ ] **5 entrées agents** (`.claude/agents/`) auditées :
  - capabilities-keeper, doctrine-keeper, scorer, reco-enricher, capture-worker → type=`subagent`
- [ ] **3 entrées skills custom NEW** (créés par ISSUE 42, 43, 44) :
  - growthcro-anti-drift, growthcro-prd-planner, growthcro-status-memory → type=`installed_custom`
- [ ] Fichier `scripts/audit_skills_governance.py` créé (mono-concern `validation`, ≤200 LOC) :
  - Lit `SKILLS_REGISTRY_GOVERNANCE.json`
  - Pour chaque entrée, grep le `invocation_proof` (file:line) — vérifie qu'il existe encore
  - Pour les `installed_external_dormant`, re-grep pour s'assurer qu'aucune invocation n'a été ajoutée (le skill reste vraiment dormant)
  - Exit 0 si registry cohérent avec code, exit != 0 si label drift détecté
- [ ] Test : `python3 scripts/audit_skills_governance.py` exit 0 après création du registry
- [ ] Stdlib only, ≤200 LOC, suit CODE_DOCTRINE §Linter contract pattern

## Technical Details

**Fichiers créés** :
- `growthcro/SKILLS_REGISTRY_GOVERNANCE.json` (~35 entrées × 10-15 lignes JSON = ~500 LOC JSON)
- `scripts/audit_skills_governance.py` (~180 LOC Python)

**Méthodologie audit** (pour produire les 17+10+5+3 entrées) :
1. Lire `skills-lock.json` → 17 IDs
2. Pour chaque ID : `grep -rn "<skill-id>" --include="*.py" .` → invocation_proof
3. Lire `skills/` directory → 10 entrées
4. Pour chaque entrée projet : check SKILL.md frontmatter + grep main entrypoints
5. Lire `.claude/agents/` → 5 entrées (frontmatter `tools:` field)
6. Ajouter 3 entrées pour les skills custom créés en ISSUE 42-44

**Cas particuliers à documenter** :
- `impeccable` : "Skill installed via skills-lock.json (pbakaus/impeccable), but invoked as Python function `run_impeccable_qa()` in `moteur_gsg/core/impeccable_qa.py:60`, not via Skill tool. Therefore type=`runtime_heuristic_python` (honest)."
- `cro-methodology` : idem (mode_1_complete.py:92)
- `emil-design-eng` : idem (emil_design_eng_audit.py + animations.py)
- Skills `obra/superpowers` : 14 dormants, vraiment jamais invoqués, type=`installed_external_dormant` candidat à drop (décision dans ISSUE-46)
- `gstack` : install échouée, type=`installed_external_dormant`, candidat à drop

## Dependencies
- ISSUE-42 (skill growthcro-anti-drift créé)
- ISSUE-43 (skill growthcro-prd-planner créé)
- ISSUE-44 (skill growthcro-status-memory créé)

(Les 3 nouvelles entrées du registry concernent ces skills.)

## Effort Estimate
- Size: M
- Hours: 8

## Definition of Done
- [ ] Registry JSON avec ~35 entrées (17 externes + 10 projet + 5 agents + 3 custom NEW)
- [ ] Schema JSON strict respecté pour chaque entrée
- [ ] Audit script Python créé (mono-concern, ≤200 LOC, stdlib only)
- [ ] `python3 scripts/audit_skills_governance.py` exit 0
- [ ] CLAUDE.md step #13 mention `SKILLS_REGISTRY_GOVERNANCE.json` à lire
- [ ] Commit conventional : `feat(governance): SKILLS_REGISTRY_GOVERNANCE.json + audit script [#45]`
