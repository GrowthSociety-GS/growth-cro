---
name: Refondre app shell (sidebar new IA + cmd-k resync + nested layout uniformized)
status: open
created: 2026-05-17T17:00:00Z
updated: 2026-05-17T17:00:00Z
github: https://github.com/GrowthSociety-GS/growth-cro/issues/72
depends_on: [67, 68]
parallel: true
conflicts_with: []
---

# Task: Refondre app shell (sidebar + cmd-k + nested layout)

> Before coding, invoke `growthcro-anti-drift` and print the pre-flight block. Source: `.claude/skills/growthcro-anti-drift/SKILL.md`.

## Description

Wave 2 begin. Première modification de code de l'epic webapp UX refonte. Applique la nouvelle Information Architecture (A2 signed-off `WEBAPP_TARGET_IA_2026-05.md`) au chrome de l'app : sidebar reflète 5 espaces + sous-menu Advanced Intelligence expand/collapse, cmd-k items resync, topbar StickyHeader uniformisé via nested layout (rendered all auth routes au lieu de home only).

**Scope tight** : refonte chrome uniquement. Pas de changement aux pages individuelles, pas de redirects 301, pas de delete /funnel. Cleanup follow-up dans B1.5.

Source :
- [`WEBAPP_TARGET_IA_2026-05.md`](../../docs/state/WEBAPP_TARGET_IA_2026-05.md) — A2 signed-off, spec complète sidebar/topbar/breadcrumbs/cmd-k
- [`MODULE_MATURITY_MODEL.md`](../../docs/state/MODULE_MATURITY_MODEL.md) — A3 signed-off, contract `Maturity` pour badges nav (optionnel B1, requis B2)
- [`WEBAPP_PRODUCT_ROUTE_AUDIT_2026-05.md`](../../docs/state/WEBAPP_PRODUCT_ROUTE_AUDIT_2026-05.md) — A1 signed-off, décisions §11

## Acceptance Criteria

- [ ] **Sidebar refondue** selon A2 §3.1 :
  - 5 espaces principaux : Command Center, Clients, Audits & Recos, GSG Studio, Advanced Intelligence
  - Advanced Intelligence en sous-menu expand/collapse (5 children : Reality, GEO, Learning, Experiments, Scent)
  - 2 utilitaires : Doctrine, Settings
  - Icônes emoji V1 (per Mathis 2026-05-17, migration Lucide reportée Phase H1)
  - Active deep-match (highlight chemin actif)
  - `/audit-gads`, `/audit-meta` RETIRÉS de la sidebar (cachés de nav, FR-25 — URL accessible directe reste)
- [ ] **`lib/cmdk-items.ts` resync** avec nouvelle IA :
  - Items par espace (5) + utilitaires (2) + public exclus
  - Actions canoniques (Add client, Run audit, New GSG brief, Open settings, etc.)
  - Autocomplete sources pour "Go to client X" (lit `clients_database.json` ou query Supabase)
- [ ] **Nested layout uniformisé** : StickyHeader + DynamicBreadcrumbs + Sidebar rendus sur **toutes** routes authenticated. Exclus : `/login`, `/auth/*`, `/privacy`, `/terms`.
- [ ] **Drop dead code** : `components/command-center/CommandCenterTopbar.tsx` + `components/ViewToolbar.tsx` + `components/Breadcrumbs.tsx` shim supprimés (cf. A1 §4 rouge)
- [ ] **Drop usages legacy** dans pages :
  - `app/page.tsx:224` : retire l'import + render de `CommandCenterTopbar`
  - `app/doctrine/page.tsx` : retire usage `ViewToolbar`, refactor en pattern `gc-topbar` inline (cohérent avec autres routes)
- [ ] **TypeScript typecheck** : `npm run typecheck` exit 0 dans `webapp/`
- [ ] **ESLint** : `npm run lint` exit 0
- [ ] **Manual smoke** : webapp dev server boot OK, naviguer `/`, `/clients`, `/audits`, `/doctrine` sans erreur runtime
- [ ] **`python3 scripts/lint_code_hygiene.py --staged`** exit 0 (aucun .py touché)
- [ ] Commit conventional : `feat(webapp): refonte app shell selon nouvelle IA [#72]`

## Technical Details

**Files Affected** (mono-concern axis : webapp frontend chrome) :

| Axis | File | Action | Approx LOC delta |
|------|------|--------|-------------------|
| frontend chrome | `webapp/apps/shell/components/Sidebar.tsx` | rewrite | +200 / -155 |
| frontend chrome | `webapp/apps/shell/lib/cmdk-items.ts` | rewrite | +180 / -100 |
| frontend chrome | `webapp/apps/shell/app/layout.tsx` | modify (nested chrome wrap) | +30 / -10 |
| frontend chrome | `webapp/apps/shell/components/chrome/StickyHeader.tsx` | extend (slot WorkerHealthBadge placeholder + user menu) | +20 |
| frontend chrome | `webapp/apps/shell/components/chrome/DynamicBreadcrumbs.tsx` | minor adjustments | +5 |
| frontend chrome (delete) | `webapp/apps/shell/components/command-center/CommandCenterTopbar.tsx` | delete | -45 |
| frontend chrome (delete) | `webapp/apps/shell/components/ViewToolbar.tsx` | delete | -60 |
| frontend chrome (delete) | `webapp/apps/shell/components/Breadcrumbs.tsx` | delete (shim) | -10 |
| frontend route | `webapp/apps/shell/app/page.tsx` | remove CommandCenterTopbar import + render | -3 |
| frontend route | `webapp/apps/shell/app/doctrine/page.tsx` | refactor ViewToolbar → inline gc-topbar | +15 / -5 |

**Total estimé** : ~10 files, ~400 LOC delta net.

**Implementation notes** :

1. **Sidebar refonte** :
   - Garde le composant `<Sidebar>` à un seul endroit
   - Structure :
     ```
     SIDEBAR
     ├── 🏠 Command Center        (`/`)
     ├── 🏢 Clients               (`/clients`)
     ├── 🔍 Audits & Recos        (`/audits`)
     ├── ✨ GSG Studio            (`/gsg/studio`)
     ├── 🧠 Advanced Intelligence (expand/collapse)
     │   ├── 📊 Reality
     │   ├── 🌐 GEO
     │   ├── 🎓 Learning
     │   ├── 🧪 Experiments
     │   └── 👃 Scent
     ├── ─────────────────
     ├── 📚 Doctrine              (`/doctrine`)
     └── ⚙️ Settings              (`/settings`)
     ```
   - Persist expand/collapse state in localStorage
   - Badges counts computed server-side (existant pattern à préserver, ex: "Audits & Recos · 3 pending")

2. **`cmdk-items.ts` resync** :
   - Drop `audit-gads`, `audit-meta` items (cachés de nav)
   - Add Advanced Intelligence parent + 5 sub-items
   - Group items par espace pour cmd-k palette categorization

3. **Nested layout** :
   - Apply Sidebar + StickyHeader via `app/layout.tsx` (root layout)
   - Excludes public routes via conditional render `if (!user)` ou via route-level layout override
   - Existing pattern home (`app/page.tsx`) supporte déjà `if (user)` conditional — extend ce pattern uniformly

4. **Dead code drop** :
   - Verify aucun import résiduel des 3 composants avant delete (grep)
   - Pour `Breadcrumbs.tsx` shim (re-export DynamicBreadcrumbs), drop simple
   - Pour `CommandCenterTopbar` et `ViewToolbar`, drop les fichiers + leurs uniques call sites

## Files Affected (flat list)

- modify: `webapp/apps/shell/components/Sidebar.tsx`
- modify: `webapp/apps/shell/lib/cmdk-items.ts`
- modify: `webapp/apps/shell/app/layout.tsx`
- modify: `webapp/apps/shell/components/chrome/StickyHeader.tsx`
- modify: `webapp/apps/shell/components/chrome/DynamicBreadcrumbs.tsx` (si nécessaire)
- modify: `webapp/apps/shell/app/page.tsx`
- modify: `webapp/apps/shell/app/doctrine/page.tsx`
- delete: `webapp/apps/shell/components/command-center/CommandCenterTopbar.tsx`
- delete: `webapp/apps/shell/components/ViewToolbar.tsx`
- delete: `webapp/apps/shell/components/Breadcrumbs.tsx`

## Out of Scope (B1.5 follow-up issue)

- /funnel route delete + link source remove dans `clients/[slug]/page.tsx:80`
- Redirects 301 via `next.config.js` (5 redirects, A2 §5)
- 7 routes anti-pattern `<main style={{ padding: 22 }}>` refactor (Phase H1 ou B1.5)
- /audit-gads et /audit-meta routes elle-mêmes (juste cacher de cmdk-items dans B1, suppression complète post-MVP)
- Worker health endpoint + heartbeat (B3 separate issue)
- Module Maturity badges sur sidebar items (B2 separate issue + post-G* loaders wiring)
- Tab `/clients/[slug]?tab=X` schema (D1 separate issue)

## Stop Conditions

- 13 canonical stop conditions
- **STOP** si `npm run typecheck` ou `npm run lint` fail post-modifications
- **STOP** si webapp dev server (port 3000) ne boot pas
- **STOP** si une route active est cassée (404 ou erreur runtime sur `/`, `/clients`, `/audits/*`, `/doctrine`, `/settings`)
- **STOP** si scope creep détecté (touchant >10 files OU >500 LOC delta OU fichiers backend Python)
- **STOP** si Module Maturity Badge wiring tenté (= B2 scope, pas B1)

## Dependencies

- depends_on: #67 (A2 signed-off) + #68 (A3 signed-off) ✅ both done
- blocks: B2 (canonical states components — depend on uniform shell), B3 (worker health badge in topbar — depend on uniform topbar), C1 (home Command Center refonte — depend on shell stable)

## Effort Estimate

- Size: L
- Hours: 8 (réduit de 10 par split B1.5)

## Definition of Done

- [ ] Tous acceptance criteria checkés
- [ ] TS typecheck + lint green
- [ ] Webapp boot + naviguer 5 routes critiques sans erreur
- [ ] Hygiene gate green (aucun .py touché)
- [ ] Screenshots dual-viewport before/after attachés à l'issue
- [ ] Commit conventional avec `[#72]` suffix
- [ ] GitHub issue closed via `gh issue close N -c "Closed: <commit-sha>"`
